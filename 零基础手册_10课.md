# 零基础手册：10 课，每课 20 分钟

> 这份手册假设你**完全不懂代码**。全程都在 Unity 界面里点，不需要读一行代码。
> 目标不是"读懂 8000 行"，而是**能演示、能讲清一条链路、能回答 5 个问题**。
> 任何一课看不懂，把课号发我（例如"第 3 课看不懂"），我用更小的步子重讲一遍。

---

## 开课前准备（约 10 分钟，只做一次）

1. **打开项目**：启动团结引擎（Unity），打开 `E:\Unity\My project`。
2. **打开场景**：在左下 Project 窗口里点 `Assets → Game → Scenes`，双击 `DEMO City Crossing`。你会看到中间出现场景画面。
3. **打开技能编辑器**：点顶部菜单 `Tools → 技能编辑器`。会弹出一个新窗口，标题是"技能编辑器"。
4. **认识窗口的三块区域**：
   - **左栏**：技能列表（一列技能名）。
   - **右栏**：技能详情——上面是"技能 ID / 技能名称"，下面是**组件列表**。
   - **右栏再往下**：帧事件表格、动画预览（要滚动才能看到）。
5. **先确认一件事**：左栏里有没有一个技能叫 `[player_uppercut] 振刀`？
   - 有 → 直接进入第 1 课。
   - 没有 → 在 Project 窗口里找到 `Assets/SkillSystem/Skills/NewSkill 2.asset`，把它拖到技能编辑器左栏的列表里。这就是"把一个技能加进技能库"。

---

## 第 1 课：什么是"资产"（20 分钟）

**本课目标**：知道"资产"是什么，以及它为什么重要。

**步骤**：

1. 在 Project 窗口里展开 `Assets → SkillSystem`。
2. 点一下 `SkillLibrary.asset`，右边的 Inspector 面板会显示它的内容：一个 `Skills` 列表，里面是一项项技能引用。
3. 再点 `Assets → SkillSystem → Skills → NewSkill 2.asset`，Inspector 里能看到：`Skill Id = player_uppercut`、`Skill Name = 振刀`、下面一长串 `Components`。

**你要理解的一句话**：

> **资产 = 项目里的一个"配置文件"**。它像一张存在硬盘上的表格。**编辑器负责改它，游戏运行时只读它。**

这就是你课题的核心：把参数从代码里搬到表格里，改表格不用重新编译代码。

**自测（把答案说给自己听）**：

1. 资产文件在项目里的哪个位置？→ `Assets/SkillSystem` 下（是项目里的文件，不是场景里的对象）。
2. 游戏正在运行时，你去改这张"表格"，游戏会立刻变吗？→ 不会，需要重新加载数据或重新运行（这正是第 7 课要讲的）。
3. `SkillLibrary` 和 `SkillConfig` 有什么不同？→ `SkillLibrary` 是**目录**（列出有哪些技能），`SkillConfig` 是**单个技能的内容**。

---

## 第 2 课：技能 = 一串"组件"（20 分钟）

**本课目标**：看懂右栏那个"组件列表"是什么。

**步骤**：

1. 在技能编辑器左栏点 `[player_uppercut] 振刀`。
2. 右栏往下看，找到"组件列表（顺序影响执行，点击 + 添加基础组件）"。
3. 列表里现在有几项，每一项上面是它的类型名（例如 `播放动画（PlayAnimationComponent）`）。
4. 点组件列表右上角的 **+**，会弹出一个菜单，里面有 5 个选项：
   - 添加 播放动画
   - 添加 判定窗口
   - 添加 伤害
   - 添加 命中反馈
   - 添加 无敌帧

**你要理解的一句话**：

> **一个技能 = 一串组件**。每个组件只负责一件事：播动画、判定有没有打中、给多少伤害、打到时播什么音效和顿帧、哪几帧无敌。**想做新技能，就是在这里加几行设置，不用写代码。**

**自测**：

1. 一个技能可以有几个组件？→ 任意多个，按顺序执行。
2. 想让技能"打人更疼"，应该改哪个组件？→ 伤害（DamageComponent）里的伤害值。
3. 组件顺序有影响吗？→ 有。播放动画必须排在判定窗口之前，因为判定窗口要读"当前播到第几帧"。

---

## 第 3 课：stateName（动画机状态名）（20 分钟）

**本课目标**：知道技能是怎么"点播"动画的，并亲手让编辑器报一次红字。

**步骤**：

1. 在技能编辑器里选中 `播放动画` 这个组件（点它那一行）。
2. 找到字段 `stateName（动画机状态名）`，现在应该是 `AT1`。
3. 打开动画机看这个名字：在 Project 窗口里双击 `Assets → Imports → momo → Player_NewAnimCtrl.controller`，会打开 Animator 窗口。在 `Base Layer` 里能找到名为 `AT1` 的状态方块。
4. **做一次实验**：回到技能编辑器，把 `stateName` 从 `AT1` 改成 `AT1x`。
5. 你会看到下面出现一行**红色的报错**，大意是"stateName「AT1x」在这个动画机里不存在——游戏里 CrossFade 会失败，表现就是按了键动作没变"。
6. 把 `AT1x` 改回 `AT1`，红字消失，变成绿色的"校验通过"。

**你要理解的一句话**：

> **stateName 就是动画机里的"频道名"**。技能要播动画，就靠这个名字去点播；名字写错，游戏里就"没这个台"，动作不变，而且**不报错**——这就是我们说的"静默失效"。

**自测**：

1. stateName 是动画片段的名字吗？→ 不是，是**动画机里的状态名**（状态上绑着某个动画片段）。
2. 写错会怎样？→ 游戏里按了键动作不变，Console 不会有明显报错；但编辑器里会红字提示。
3. 这个红字是谁做的？→ 技能编辑器自己做的校验（读动画机的状态列表来比对）。

---

## 第 4 课：判定窗口（第几帧能打中）（20 分钟）

**本课目标**：知道"判定窗口"是什么，以及它在场景里长什么样。

**步骤**：

1. 在技能编辑器里选中 `判定窗口` 组件。
2. 找到这几个字段：
   - `startFrame（判定起始帧）= 5`、`endFrame（判定结束帧）= 15` → 意思是"动画播到第 5 帧到第 15 帧之间，这一下才可能打到人"。
   - `shape（检测形状）` = Sphere / Capsule / Box（球 / 胶囊 / 盒子）。
   - `radius（检测半径）`、`capsuleLength（刃长）`、`offset（相对节点的偏移）`。
   - `attachNodeName（★绑定节点名）` = 一串骨骼路径（`Armature/.../J_Bip_R_Hand/Weapon_Socket`）→ 意思是"判定盒挂在这根骨骼上，跟着手/武器动"。
3. **看它长什么样**：技能编辑器里勾选"在 Scene 视图显示判定盒"，然后在 Scene 窗口里就能看到那个线框（球或胶囊），还能拖动上面的手柄改半径和朝向。
4. 切换 `shape` 从 Sphere 改成 Capsule，观察线框形状变化。

**你要理解的一句话**：

> **判定窗口 = "第几帧到第几帧之间能打中"**，判定盒挂在武器骨骼上跟着动，所以必须真的挥到才命中；如果挂在角色身上，就会出现"隔空打中"。

**自测**：

1. 判定窗口的起止帧是相对什么说的？→ 相对这一段的动画帧（动画播到第几帧）。
2. 为什么要把判定盒绑到骨骼上？→ 让判定跟着武器走，避免隔空判定、也让高速挥砍更准。
3. 每段连招的判定时间要配在哪？→ 就是这一段技能/连招的"判定窗口"组件里。

---

## 第 5 课：帧事件（动画里的"闹钟"）（20 分钟）

**本课目标**：知道帧事件是什么，并亲手做一条"永远不会触发"的错误配置。

**步骤**：

1. 在技能编辑器里往下滚，找到"帧事件表格"。
2. 表格有几列：`帧号`、`音效ID（▾ 从库选择）`、`音量`、`试听`。
3. 现在应该有 1 行（例如 `帧 8`、音效 ID `k`）。点那一行的 **▶** 试听一下——如果听不到声音，说明音效 ID 在库里没有对应文件。
4. 点"选择▾"按钮，可以从下拉里挑一个音效 ID（下拉里的 ID 来自项目里的音效库）。
5. **做一次实验**：把帧号从 8 改成 `999`。你会看到红色提示，大意是"这些帧事件永远不会触发……这个技能最多只能走到第 N 帧"。
6. 把帧号改回 8。
7. 顺手试一次"按帧添加"：把编辑器里的时间轴拖到某一帧，点"在第 N 帧添加事件"，表格里会多一行。

**你要理解的一句话**：

> **帧事件 = 在第 X 帧放一个闹钟**，到点播一次音效。如果帧号超过了这段动画能走到的总帧数，闹钟永远不会响，而且游戏里**不会报错**——所以编辑器要提前把它标红。

**自测**：

1. 帧事件的作用是什么？→ 在指定帧触发音效（也可以理解为表现事件）。
2. 为什么帧号 999 会红？→ 这段动画最多只播到第 N 帧，999 帧永远走不到。
3. 编辑器怎么知道"最多走到第几帧"？→ 用技能时长 × 帧率（再考虑播放速度）算出来的。

---

## 第 6 课：伤害与命中反馈（20 分钟）

**本课目标**：知道"改哪个数字能让技能更疼"，以及命中时会发生什么反馈。

**步骤**：

1. 在技能编辑器里选中 `伤害` 组件，看 `damage（伤害数值）` 现在是 `10`。
2. 选中 `命中反馈` 组件，看这几个字段：`soundIdPrefix（命中音效前缀）`、`hitStopDuration（顿帧时长·秒）`、`hitStopTimeScale（顿帧缩放）`。
3. 记住这两个数字：**伤害 10**、**顿帧 0.05 秒**——第 7 课要用。

**你要理解的一句话**：

> 命中敌人时发生三件事：**扣血**（用伤害组件的数值）、**播放命中音效**（按前缀随机选一条，例如 `hit_01`）、**顿帧**（画面短暂变慢，打起来更"有手感"）。

**自测**：

1. 想让技能伤害翻倍，改哪里？→ 伤害组件的 `damage`。
2. 顿帧是什么？→ 命中瞬间把游戏时间变慢一小会儿（例如 0.05 秒、10% 速度），制造打击感。
3. 命中音效为什么用"前缀"而不是具体 ID？→ 前缀可以随机取一条变体（`hit_01`~`hit_07`），听起来不重复。

---

## 第 7 课：完整链路——改一个值，游戏里生效（20 分钟）

**本课目标**：亲手跑通一次"编辑器修改 → 资产 → 运行时生效"，这是整个课题最核心的一句话。

**动手做（按顺序，别跳步）**：

1. 在技能编辑器里选中 `伤害` 组件，把 `damage` 从 **10 改成 60**。
2. 点窗口最下方的按钮 **"保存所有修改"**。Console 里会出现 `[SkillEditor] 已保存`。
3. 点 Unity 顶部的 **▶ Play** 进入运行模式。
4. 在游戏画面里**点鼠标右键**（这是当前用来放这个技能的测试按键）。Console 里应该出现：

   ```
   [Skill] player_uppercut: Released → Running
   ```

   这一行就是"技能确实执行了"的证据。
5. 观察敌人：应该会**掉血**（敌人头顶/屏幕上的血条变短）、被**击退**或进入**僵直**。

   > 注意：这里的伤害数字不会飘出来——飘字只在"普通攻击（鼠标左键连击）"时出现。技能伤害看**敌人血条**最直观，所以改成 60 之后，血条掉得比 10 的时候明显更多。

6. 退出 Play，把 `damage` 改回 10，保存，再进 Play 对比一次。你会发现：**同一次攻击，血条掉的血量跟着你写的数字走**。

**你要背下来的这句话**（答辩开场就用它）：

> 技能参数存在 ScriptableObject 资产里。编辑器改完参数写回资产、运行时读同一份资产，所以**改配置不需要重新编译代码**——这就是"数据驱动"。

**自测**：

1. 改了值但没点"保存所有修改"，游戏里会变吗？→ 不会（要么没写回资产，要么重新 Play 才读）。
2. 怎么证明技能真的执行了？→ Console 里的 `[Skill] 技能ID: Released → Running`。
3. 为什么技能伤害看不到飘字？→ 飘字是普通攻击那条链路触发的，技能这条链路看敌人血条。

---

## 第 8 课：把三个演示动作练熟（20 分钟）

答辩时最值钱的是"手上有活"。把下面三个动作各练 3 遍，每个控制在 **30 秒以内**：

**动作一：改参数 → 生效**

打开技能编辑器 → 选中技能 → 改伤害 10→60 → 保存 → Play → 右键放技能 → 指出 Console 的 `Released → Running` 和敌人血条变化 → 退出 Play 改回 10。

**动作二：写错状态名 → 编辑器报错**

选中播放动画组件 → `stateName` 改成 `AT1x` → 指出红字提示"这个状态名不存在，游戏里动作不会变" → 改回 `AT1`。

**动作三：帧号越界 → 编辑器报错**

帧事件表格 → 把帧号改成 `999` → 指出红字"永远不会触发"+ 它给出的最大可达帧数 → 改回 8。

**练完之后，你应该能不看笔记说出这句话**：

> 我的编辑器不只是"能改参数"，它还会**检查配置对不对**：状态名写错、帧号超出、音效 ID 在游戏里没有，都会当场红字报出来——把错误挡在编辑阶段，而不是等游戏里不生效才发现。

---

## 第 9 课：5 个必答问题（20 分钟）

把答案念 3 遍，念到不用看稿：

**问 1：你的课题做什么？**
> 做一个 Unity 编辑器扩展工具——连招配置编辑器，让策划不写代码就能配连招：每段的动画、判定起止时间、伤害、连招窗口、位移、动画事件。另外做一个动作游戏 Demo，用来验证配出来的东西在游戏里真的生效。

**问 2：编辑器怎么用？**
> 打开 Tools 菜单里的技能编辑器，左边选技能，右边配组件（播放动画、判定窗口、伤害、命中反馈、无敌帧），下面是帧事件表格，改完点"保存所有修改"。（**边说边演示动作一**）

**问 3：改完怎么让游戏生效？**
> 参数存在 ScriptableObject 资产里；编辑器写回资产，运行时读资产，两边只通过资产通信，所以不用重新编译代码。技能跑起来 Console 会打 `Released → Running`。

**问 4：遇到过什么坑？**
> 最麻烦的是"配置错了但不报错"：帧号配到动画走不到的帧、状态名拼错、音效 ID 在运行时库里没有——这三种在游戏里都只是"功能不生效"，Console 一声不吭。所以我把校验做进了编辑器，当场红字提示。（**演示动作二或三**）

**问 5：还有什么没做完？**
> 任务书里的角色跳跃、敌人巡逻、连击数界面还在计划中；编辑器的连招步骤排序与连招窗口、位移参数正在补；技能系统的单元测试也排在计划里。这些在论文的展望部分有写。

---

## 第 10 课：3 分钟开场白（20 分钟）

把下面这段话抄下来，对着镜子念 3 遍（括号里是动作提示）：

> 各位老师好，我的课题是基于 Unity 的动作游戏与编辑器工具的设计与实现。
>
> 我做的事情是：开发一个连招配置编辑器，让连招的动画、判定时间、伤害、连招窗口和位移都能在编辑器里配，不写代码就能改内容；同时做一个动作游戏 Demo 来验证配置是否真的生效。
>
> （打开编辑器）我的编辑器有三块：左边是技能列表，右边是组件配置，下面是帧事件表格。一个技能就是一串组件——播放动画、判定窗口、伤害、命中反馈、无敌帧。
>
> （改伤害 10→60，保存，Play，右键放技能）我把伤害从 10 改成 60，保存后进游戏放技能，Console 打出技能已执行，敌人血条掉得比刚才更多——这就是"编辑器配置、运行时执行"的链路。
>
> （把 stateName 改错）另外，编辑器会做配置校验：状态名写错、帧号超出动画长度、音效 ID 在游戏里不存在，它都会当场红字报错，不用等到游戏里才发现没效果。
>
> 这就是我的课题：工具是研究对象，游戏是验证平台。谢谢老师。

---

## 附录 A：卡住时的排查

| 现象 | 先看哪里 |
|---|---|
| `Tools` 菜单里没有"技能编辑器" | 编辑器脚本没编译完，等 Unity 右下角转圈结束；还不行就把 Console 的红色报错发我 |
| 技能编辑器左栏没有 `player_uppercut` | 把 `Assets/SkillSystem/Skills/NewSkill 2.asset` 拖进左栏列表 |
| 按右键没反应、Console 没有 `[Skill]` | 场景里要有挂 SkillManager 的 `SkillSystem` 对象（场景里确实有）；再看那个技能的 ID 是否和右键调用的 ID 一致 |
| 技能有日志但敌人不掉血 | 判定窗口的起止帧、绑定节点、检测半径可能不对；也可能是敌人不在检测层上（LayerMask） |
| 音效没声音 | 编辑器里如果音效 ID 显示红字，说明运行时音效库（场景里 SoundManager 挂的库）里没有这条 ID |
| 帧事件不响 | 帧号是否在"最多可达帧数"以内；音效 ID 是否在运行时库里 |
| 看不到"敌人掉血"的日志 | 项目的日志开关默认是关的（`GameLog.Enabled = false`），敌人扣血只写进日志开关里，不打印；**看敌人血条就行**。技能执行的那行 `[Skill] ... Released → Running` 一定会打印 |

## 附录 B：术语小词典（10 个词，一句话版）

| 词 | 一句话解释 |
|---|---|
| 资产（ScriptableObject） | 项目里的配置文件，编辑器改、运行时读 |
| SkillLibrary | 技能目录，列出有哪些技能 |
| SkillConfig | 单个技能的配置（ID、名称、组件列表） |
| 组件（Component） | 技能里的一件事：播动画 / 判定 / 伤害 / 反馈 / 无敌 |
| stateName | 动画机里的状态名，技能靠它点播动画 |
| 判定窗口 | 第几帧到第几帧能打中，判定盒绑在骨骼上 |
| 帧事件 | 第 X 帧触发的"闹钟"，用来播音效 |
| 顿帧 | 命中瞬间时间变慢，制造打击感 |
| 静默失效 | 配置错了但游戏不报错，只是功能不生效 |
| 运行时（Runtime） | 游戏跑起来的那个状态下执行的代码，只读资产 |

## 附录 C：这 10 课分别对应答辩的哪个问题

| 课 | 对应答辩问题 |
|---|---|
| 1、2 课 | "你的工具有什么功能" |
| 3、4、5、6 课 | "每段连招能配什么""判定怎么做的" |
| 7 课 | "编辑器改完，游戏里怎么生效"（高频问题） |
| 8 课 | 现场演示 |
| 9 课 | 老师的常规追问 |
| 10 课 | 开场陈述 |

> 学完这 10 课，你已经能覆盖答辩 80% 的问题。剩下的深度追问（比如为什么用 SerializedObject、动画覆盖怎么实现）属于加分项，答不上来就说"这块我回去核对代码后补充，我的理解是……"，不要硬编。

## 附录 D：不用写代码的"动手改"清单（五个小实验）

读十遍不如自己改一次。下面五个实验全部在 Unity 界面里做，不写代码，每个做两遍：

| 实验 | 怎么做 | 观察什么 |
|---|---|---|
| 1. 加一个组件 | 选中技能 → 组件列表点 **+** → 选"伤害" | 列表多了一行"伤害"；点它，能改里面的伤害数值 |
| 2. 删一个组件 | 选中刚加的那一行 → 点组件列表的 **-** | 那一行消失（若留下"空槽位"，说明这是已知的删除小问题，论文展望里写了改进方向） |
| 3. 拖动组件顺序 | 按住组件左边的手柄上下拖 | 顺序变了。**记住：播放动画要排在判定窗口前面**，否则判定读不到"当前第几帧" |
| 4. 改 stateName 看红字 | 选中播放动画 → stateName 改成 `AT1x` | 出现红字"这个状态名不存在"；改回 `AT1` 红字消失 |
| 5. 改帧号看红字 | 帧事件表格 → 帧号改成 `999` | 出现红字"永远不会触发"，并告诉你这段最多能走到第几帧 |

做完这五个实验，你就亲手验证了"编辑器能改、改错了会报错"这两件事——答辩演示动作一、二、三就是从这里来的。

## 附录 E：如果你以后想"真的读懂代码"

答辩需要的是"能演示、能讲清"，不是"能独立写编辑器工具"。等你答辩结束（12 月以后），如果想为了面试或工作真的读懂代码，可以按这条顺序来，效率最高：

1. **先读数据类，再读界面**：`SkillConfig.cs`、`SkillLibrary.cs`、`PlayAnimationComponent.cs` 先看（它们是"数据结构"），编辑器代码就变成"给已知的数据结构画界面"，简单很多。
2. **按数据流读，不要按代码顺序读**：一次只跟一条线（例如"左栏技能列表"：打开窗口 → 加载库 → 画列表 → 点 + 号新建 → 写回资产）。
3. **在关键回调里加一句 `Debug.Log`**，跑一遍看日志，比读十遍都清楚。
4. **动手改一个小功能**（例如给技能加一个"描述"字段并在界面上显示），改通了就说明真懂了。

这四步做完，大约需要 2~3 周（每天 1 小时），属于"超出答辩需要、但对职业发展有用"的投入——**建议放到答辩之后再做**。
