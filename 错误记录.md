# 错误记录（Bug Log）

> **这份文档记什么**：项目里踩过的坑，一条一条记"现象 → 根因 → 证据 → 修复 → 验证"。
> **为什么要单独建**：① 答辩问"你遇到过什么困难"时直接从这里挑；② 论文第五章"关键问题解决"的素材；③ 同一个坑不踩第二次。
> **怎么用**：新条目追加到最上面（最新的在前）。每条都要写清"证据"（哪个文件、哪一行），否则过两个月自己也看不懂。

---

## 记录模板（复制这一段写新条目）

```
## BUG-编号 一句话标题（日期，状态）
- 【现象】用户看到什么
- 【复现步骤】怎么操作能稳定触发
- 【根因】真正的原因（一条或几条）
- 【证据】文件路径 + 行号 / 资产里的关键字
- 【修复方案】怎么改的，为什么这么改
- 【改动文件】列出来
- 【验证步骤】改完怎么证明好了
- 【状态】未修 / 已修待验证 / 已验证
- 【论文可用性】能不能写进论文哪一章
```

---

## BUG-003 角色"挥空也能打中怪物"：普通连击用角色中心大球判定（2026-09-22，已修待验证）

- **【现象】** 站在怪物前方两米左右挥刀，刀明显没碰到也会造成伤害；侧面、背后挥砍同样能中。
- **【根因】** `PlayerCombat.PerformHitDetection()` 用的是**角色中心的球形检测**：球心取 `transform.position + forward × 1.5`，半径取 `attackRadius`（`ComboData.asset` 里是 2.5）→ 实际覆盖范围是"身后 1 米到身前 4 米、左右各 2.5 米"的巨大球体。**刀有没有挥到完全不参与判定**，也没有任何"上一帧→这一帧"的连续性检测，所以既误判（没碰到也中）又有漏判风险（高速挥砍可能跳过去）。
- **【证据】**
  - 旧代码 `Assets/Scripts/Player/PlayerCombat.cs`：`Vector3 origin = transform.position + transform.forward * 1.5f; Physics.OverlapSphere(origin, AttackRadius, enemyLayer);`
  - 数据 `Assets/Scripts/Data/ComboData.asset`：`attackRadius: 2.5`
  - 任务书技术要求第（4）条："命中检测使用 SphereCast 或 Capsule Collider，避免漏判或误判"——旧实现两条都没对上。
- **【修复方案】** 普通连击的判定改成【武器判定胶囊 + 帧间扫掠】两条检测叠加。判定胶囊按优先级取三档来源（Scene 里用颜色区分）：
  1. 🟢 **武器上自己摆的 `CapsuleCollider`（推荐，优先级最高）**：直接读它的 `center / radius / height / direction`，换算成世界空间的胶囊两端点与半径 —— **判定形状与你在 Inspector 里看到的碰撞体完全一致**，还能用 Unity 自带的 Edit Collider 直接拖形状，也正好对上任务书"命中检测使用 Capsule Collider"。
  2. 🟠 **按武器网格自动拟合**（没放碰撞体时）：合并武器所有 `Renderer` 的包围盒 → 取最长轴作为刀身方向 → 胶囊刚好包住整把武器；半径可用 `_weaponRadiusScale` 缩放。
  3. 🟡 **节点前向兜底**（连网格都拿不到时）：用武器节点前向 + 固定 `_weaponBladeLength / _weaponCastRadius`。
  两条检测本身不变：`Physics.OverlapCapsule(p0, p1, 半径, 敌人层)`（Capsule Collider 语义，治误判）+ `Physics.SphereCastAll(上一帧胶囊中心 → 这一帧)`（SphereCast 语义，治高速挥砍漏判）。
  配套：每帧在 `LateUpdate` 里记录胶囊中心；同一次挥击对同一敌人只结算一次（`hitEnemies` 去重）；Scene 视图按武器网格画出**胶囊线框**（两个端面圆 + 四条母线，橙色=贴合武器网格，黄色=兜底形状）；找不到武器时自动退化为"节点前向 + 固定刃长"，再不行才回到旧球形判定，全程不报错。
- **【修正记录】** 第一版实现用的是"武器节点前向 + 固定刃长/半径"，因为武器是运行时由 `WeaponAttachment` 挂到手上的，节点朝向与实际刀身不一致，胶囊会歪到武器两侧。**已改为直接按武器网格包围盒拟合**，所见即判定体积。为此在 `WeaponAttachment` 里加了 `WeaponPrefab` 属性与 `GetWeaponWorldMatrix()`（编辑器里没有实例时也能预览胶囊位置与朝向）。
- **【新增可调参数】**（玩家身上的 PlayerCombat，全部在 Inspector 里）
  - `_useWeaponHitbox` = 勾选（**取消勾选即回到旧的"角色中心大球"**，用于论文里的对照实验）
  - `_weaponNodeName` = 留空（留空 = 自动用 `WeaponAttachment` 挂上的武器；也可填节点名）
  - `_weaponRadiusScale` = 1（1 = 刚好包住武器；觉得判定太苛刻就调到 1.2~1.5）
  - `_weaponRadiusMin` = 0.08（半径下限，避免细剑判定过窄）
  - `_weaponCastRadius` / `_weaponBladeLength` = 兜底值（完全拿不到武器网格时才用）
- **【改动文件】** `Assets/Scripts/Player/PlayerCombat.cs`
- **【验证步骤】** ① 选中玩家（或进 Play）看 Scene 视图：判定线框应与武器上那个 `CapsuleCollider` **重合**（🟢 绿色；若显示橙色说明没读到碰撞体、走了网格拟合；黄色说明连网格都没拿到）；② 站在怪物前方约 2 米挥刀 → 不应命中；③ 走到刀能够到的位置挥刀 → 正常命中（伤害数字、命中音效、顿帧都在）；④ 侧面/背后挥刀 → 不应命中；⑤ 连挥三段 → 不漏判、同一次挥击不重复结算；⑥ 取消勾选 `_useWeaponHitbox` → 手感回到旧的"大球"版本（对照）。
- **【为什么用"碰撞体"而不是 OnTriggerEnter】** 触发器事件依赖物理帧、高速挥砍容易"穿过去"漏判，而且同一帧多次进出容易重复结算；用 `OverlapCapsule` + `SphereCastAll` 主动查询既不会漏判（扫掠覆盖两帧之间的路径），也能靠"同一次挥击去重"保证只结算一次。碰撞体保持 `isTrigger = true` 不影响查询（`Physics.queriesHitTriggers` 默认开启），`WeaponAttachment` 也会自动把它设成 Trigger。
- **【状态】** 已改，编译检查通过，**待 Unity 里运行验证**。
- **【论文可用性】** 直接对应任务书技术要求第（4）条，可写进第五章"关键问题解决"：把"角色中心球形检测"与"武器骨骼胶囊 + 帧间扫掠"做成对照实验（同一场景下"2 米外 / 贴近 / 侧面 / 高速连击"四组观测），并说明体积判定与连续性检测分别解决误判与漏判。
- **【本轮未动】** 弹反检测（`TryParry` 里的 `OverlapSphere`，`_parryRadius = 2`）仍是角色中心球——它属于防御判定、范围大一点可接受；如需一并收紧，可按同样方式改成武器骨骼判定。

---

## BUG-002 怪物"隔空"打中玩家：命中判定用中心点距离，没按碰撞体体积算（2026-09-22，已修待验证）

- **【现象】** 玩家离怪物还有半米多，就会被打到，看起来像"隔空挨打"，闪避也没意义。
- **【根因】** `EnemyAI` 的所有范围判定都基于 `Vector3.Distance(怪物中心, 玩家中心)`，完全没有使用碰撞体尺寸；伤害判定是 `dist ≤ _attackRadius × 0.8 = 1.8 × 0.8 = 1.44 米`。
- **【证据】**
  - `Assets/Scripts/Enemy/EnemyAI.cs`：`DistanceToPlayer()`（中心点距离）、`TryDealDamage()`（`dist > _attackRadius * 0.8f` 直接返回）。
  - 场景 `DEMO City Crossing`：敌人身上的 `BoxCollider` 尺寸 `0.987 × 2.276 × 1.0`（水平半径 ≈ **0.49m**）；玩家 `Assets/Game/Prefabs/momo.prefab` 的 `BoxCollider` 尺寸 `0.436 × 1.685 × 0.393`（水平半径 ≈ **0.22m**）。
  - 两者贴身时的中心距 ≈ 0.49 + 0.22 = **0.71m**，而判定允许 **1.44m** → 多出约 **0.73m** 的空隙，这就是"隔空"的来源。
- **【修复方案】** 把伤害判定从"中心点距离"改成"碰撞体表面距离"：
  `表面距离 = 中心点距离 − 自己水平半径 − 对方水平半径`，`表面距离 ≤ 容差` 才算命中。
  新增两个 Inspector 可调字段：`_useSurfaceHitCheck`（默认勾选，取消即回到旧行为，方便对比）与 `_hitSurfaceTolerance`（默认 0.2 米，越小越严格）。
  半径取自两个碰撞体包围盒的水平半尺寸（`Collider.bounds.extents`），随模型缩放自动适配。
- **【改动文件】** `Assets/Scripts/Enemy/EnemyAI.cs`
- **【验证步骤】** ① 站在怪物身旁、肉眼还有缝隙时 → 不应再掉血；② 贴到怪身上 → 正常掉血；③ 把 `_hitSurfaceTolerance` 从 0.2 改到 0.4 → 判定明显变宽松，说明参数生效；④ 取消勾选 `_useSurfaceHitCheck` → 恢复旧手感（用于对照实验）。
- **【状态】** 已改，编译检查通过，**待 Unity 里运行验证**。
- **【论文可用性】** 可写进第五章"关键问题解决"：中心点距离判定 vs 碰撞体体积判定的差异，以及用碰撞体尺寸把命中收紧到"贴身"的做法；也可以作为第三章"需求分析"里"命中判定准确性"的一个具体案例。

---

## BUG-001 技能播完或被打断后，角色卡死在技能动画里，不回待机（2026-09-22，已修待验证）

- **【现象】** 用技能编辑器配好的技能（`player_uppercut` 振刀），进游戏按右键释放后，动画播完停在最后一帧不动，不会自动回到 Idle；按移动键也走不出来。
- **【复现步骤】** 打开 `Tools → 技能编辑器` → 左栏选中 `[player_uppercut] 振刀` → 点 Play → 鼠标右键放技能 → 等 2 秒 → 角色卡在攻击动画的最后一帧。
- **【根因】两条，缺一不可：**
  1. **技能播放的动画机状态是个"死胡同"**：`Player_NewAnimCtrl.controller` 里 `AT1` 状态的 `m_Transitions: []`（空的），而 `at2 / at3 / at4 / Locomotion / Idle` 都有出口过渡。技能用 `CrossFadeInFixedTime("AT1")` 进去以后，动画机没有任何过渡能自己走出来。
  2. **技能结束时没有交还动画控制权**：`PlayAnimationComponent.OnEnd` 与 `OnInterrupt` 原本是空实现（原文件第 217-218 行），技能实例结束时只清理数据，不碰 Animator。而玩家状态机是靠"设参数"（`Movement` 浮点 / `Run` 布尔 / 各种 Trigger）驱动动画的，当动画机停在 `AT1` 上时这些参数触发不了任何过渡 —— 所以**按键也救不回来**。
- **【证据】**
  - `Assets/Imports/momo/Player_NewAnimCtrl.controller`：`m_Name: AT1` 状态下 `m_Transitions: []`（对比 `at2`、`at3`、`at4`、`Locomotion`、`Idle` 都有 `- {fileID: ...}` 条目）。
  - `Assets/Scripts/SkillSystem/阶段3/PlayAnimationComponent.cs`：`public override void OnEnd(SkillContext ctx) { }`、`OnInterrupt(...) { }`。
- **【修复方案】用事件通信交还控制权（方案 C）**
  1. 新增 `Assets/Scripts/SkillSystem/SkillEvents.cs`：静态事件 `AnimationReturnRequested(caster, 建议状态名)`，作为技能与角色之间的轻量通道。
  2. `PlayAnimationComponent` 新增字段 `returnStateName`（留空 = 由角色自己决定，推荐留空），并在 `OnEnd` / `OnInterrupt` 里抛出该事件。
  3. `PlayerAnimController` 在 `OnEnable` / `OnDisable` 成对订阅该事件（只处理"自己身上"的技能），收到后调用新的 `ReturnToLocomotion()`：留空时按 `Movement` 参数自动选 `Idle`（速度≈0）或 `Locomotion`（移动中），用 `CrossFadeInFixedTime` 平滑切回。
  4. 技能编辑器为 `returnStateName` 加了中文标签与悬停说明，策划可在界面上直接填。
  5. **为什么这么设计**：技能是数据（配置在 ScriptableObject 上），不应该知道角色有几种状态；"什么时候交还控制权"由技能决定，"回去播什么"由角色决定。这也正好对应任务书"通过事件通信实现状态打断"的要求。
- **【改动文件】** `Assets/Scripts/SkillSystem/SkillEvents.cs`（新增）、`Assets/Scripts/SkillSystem/阶段3/PlayAnimationComponent.cs`、`Assets/Scripts/Player/PlayerAnimController.cs`、`Assets/Editor/Tool/编辑器/动作工具/SkillEditorWindow.cs`
- **【验证步骤】** ① 放技能 → 等技能结束 → 角色应自动回到待机，按 W 能立刻走动；② 再放一次技能，中途按 K 打断（或让敌人打你一下）→ 同样能正常回待机；③ 连续放两次技能 → 不叠加、不卡死。
- **【状态】** 代码已改，编译检查通过，**待 Unity 里运行验证**。
- **【论文可用性】** 可写进第五章"关键问题解决"：技能系统与角色状态机的动画控制权交接（事件通信解耦），以及"静默失败"类问题的定位方法（先看资产里的过渡配置，再看结束回调）。

---

## 待跟进（已知但本轮没修）

1. **动画机侧建议再补一条兜底过渡**：给 `AT1`（以及以后新增的技能状态）加一条 `→ Locomotion` 的 `Has Exit Time = 1.0` 过渡。代码已经能交还控制权，但动画机里有出口过渡更保险（万一有别的路径 CrossFade 进去）。
2. **`player_uppercut` 的 `duration = 2` 秒，而 `AT1` 动画不到 1 秒**：动画播完后技能还要"空转"一秒多，手感上会发愣。建议把 `duration` 调到与动画长度接近（编辑器里会提示"这个技能最多能走到第 N 帧"）。
3. **技能结束后不还原 AnimatorOverrideController**（历史决策，为避免二次 rebind 打断回 Idle）——现在的方案靠"下次技能开始时规范化为原始 controller"兜底，暂时保留。
4. **编辑器删除组件会留"空槽位"**（`DeleteArrayElementAtIndex` 对引用型元素只置空不缩短数组）——已在开题报告里写成"改进方向"，答辩前可以顺手修。
5. **05 冲击（起跑撞人）的动画分支仍是中心距判定**：`Attack05Routine` 里 `dist < _attackRadius * 1.5f` 只决定播"撞到"还是"未撞到"两个动画（**不结算伤害**，伤害统一走 `TryDealDamage`）。如果也想让它按体积判定，把那一行换成 `IsPlayerInHitReach(...)` 即可。
