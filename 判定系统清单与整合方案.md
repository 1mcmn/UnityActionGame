# 判定系统清单与整合方案

> 用途：把项目里散落的"判定代码"一次看清——**谁在判定谁、用什么方式、参数在哪、重复在哪**。
> 这份文档可以直接变成论文第三章（系统分析）与第五章（关键问题解决）的素材。
> 排查日期：2026-09-22

---

## 一、总览：现在其实有四套判定口径

| # | 链路 | 触发方式 | 判定方式 | 目标筛选 | 去重 | 结果解析 |
|---|---|---|---|---|---|---|
| 1 | **玩家普通连击** → 怪物 | 动画事件（挥击帧一次） | 武器胶囊 `OverlapCapsule` + `SphereCastAll` 扫掠（脚本 `PlayerCombat.cs:294/303`） | `PlayerCombat.enemyLayer`（Inspector） | `_hasHitThisSwing` + `hitEnemies` | 直接 `Enemy.TakeDamage`（`PlayerCombat.cs:266`） |
| 2 | **玩家技能** → 怪物 | 帧窗口（起止帧内逐帧） | 判定盒 `OverlapSphere/OverlapCapsule/OverlapBox` + 帧间三采样扫掠（`HitWindowComponent.cs:208/212/216`） | `HitWindowComponent.targetMask`（默认 128） | `HitWindowState.hitTargets` | `HitResolver.Resolve` + `IHitReactable`（`HitWindowComponent.cs:246/307`） |
| 3 | **玩家弹反** → 怪物 | 按键 + 状态 | 角色中心球 `OverlapSphere(origin, _parryRadius=2)`（`PlayerCombat.cs:690`） | `PlayerCombat.enemyLayer` | 无 | 直接调 `EnemyAI.OnParried()`（`PlayerCombat.cs:698`），**不走 HitResolver** |
| 4 | **敌人攻击** → 玩家 | 协程延迟 `_hitDelay` 后一次 | **无判定体积**：碰撞体表面距离 ≤ 容差（`EnemyAI.cs:465-467`） | 无（只算玩家距离） | 无 | 直接 `ThirdPersonController.TryTakeDamage`（`EnemyAI.cs:452`） |
| 5 | 敌人被弹反时的自我伤害 | 弹反事件 | 无 | 无 | 无 | **硬编码 `TakeDamage(30f)`**（`EnemyAI.cs:684`） |

**一句话结论**：技能那条链路（#2）是"数据驱动 + 帧窗口 + 体积判定 + 结果解析"的规范做法；普通连击（#1）是"代码硬编码 + 体积判定"；弹反（#3）和敌人攻击（#4）还停在"中心球 / 距离阈值"的阶段。

---

## 二、受击方现状（hurtbox）

| 对象 | 受击入口 | 体积 | 无敌 / 格挡 | 备注 |
|---|---|---|---|---|
| 玩家 | `ThirdPersonController.TryTakeDamage`（`ThirdPersonController.cs:84`）→ `PlayerCombat.TakeDamage`（`PlayerCombat.cs:628`） | 身上的 BoxCollider（0.44×1.69×0.39），**但敌人攻击链路并不用它** | `PlayerCombat.isInvulnerable` 布尔开关（闪避/格挡/死亡时由状态机设 `SetInvulnerable(true)`） | 没有独立的 hurtbox 概念 |
| 怪物 | `Enemy.TakeDamage`（`Enemy.cs:134`） | 身上的 BoxCollider（0.99×2.28×1），由攻击方的 Overlap 查询命中 | `_invulnerabilityTimer = 0.2s` + 僵直累加 `_currentPoise` | 去重靠"时间无敌"而不是"同一次挥击" |

---

## 三、非空间判定（手感规则，容易和上面混在一起）

| 位置 | 判定内容 | 说明 |
|---|---|---|
| `ThirdPersonController.cs:514` | 连击窗口：`animCtrl.GetNormalizedTime01() >= combat.ComboWindowStart` | 动画进度百分比判定 |
| `PlayerCombat.ComboWindowPercent / IsInGrace` | 连招窗口 + 残响窗口 | 数据来自 `ComboData` 资产 |
| `PlayerCombat.ConsumeBufferedCombo()` | 输入缓冲（`_comboBuffered`） | 状态机在窗口内才允许缓冲 |
| `PlayerAnimController.IsInState("Idle")` | "当前能不能起手攻击" | 用动画状态名做前置条件 |
| `EnemyAI.DistanceToPlayer()` + `_detectRadius / _closeRadius / _attackRadius` | AI 感知与起手决策 | 中心点距离，与攻击判定无关 |
| `TargetFinder.FindNearest`（`TargetFinder.cs:15`）+ `ThirdPersonController.cs:669` | 找最近敌人（锁定/朝向用） | `OverlapSphere`，掩码来自 `combat.EnemyLayer` |

---

## 四、问题清单（"乱"具体乱在哪）

1. **四套判定口径并存**（见第一节表）：体积+扫掠 / 体积无扫掠 / 中心球 / 表面距离。
2. **三套去重机制互不知情**：`_hasHitThisSwing`（普攻）、`hitTargets`（技能）、`_invulnerabilityTimer`（敌人受击）。同一个目标可能被两套判定叠加结算。
3. **两套结果解析**：`HitResolver + IHitReactable`（技能链路，**而且全项目没有任何类实现这个接口**）vs 玩家侧的 `isInvulnerable` 布尔开关 + 弹反直接调 `OnParried`。
4. **两处 LayerMask 来源**：`PlayerCombat.enemyLayer`（Inspector 配）与 `HitWindowComponent.targetMask`（默认硬编码 128），靠注释"请保持一致"维系——迟早漂移。
5. **可配性不一致**：技能伤害/判定/反馈都在资产里可配；普通连击的伤害数组、判定半径、顿帧时长写在代码字段里；敌人弹反伤害 **30 点直接硬编码**（`EnemyAI.cs:684`）。
6. **命名看不出归属**：`attackRadius`（玩家普攻）、`_attackRadius`（敌人 AI 起手）、`_parryRadius`、`radius`（技能判定盒）、`enemyLayer`、`targetMask` —— 同名不同义、同义不同名。
7. **受击体积没有显式存在**：攻击方各自用不同方式找目标（Layer 过滤 / 距离 / 直接取组件），没有统一"受击盒"的概念。

---

## 五、目标形态（理清之后长什么样）

```
攻击方 hitbox（在判定帧/挥击帧开启）
  ├─ 玩家：普通连击、技能   → 统一「判定体积 + 帧窗口 + 帧间扫掠」
  └─ 敌人：攻击动作         → 同样给一个判定体积（手/爪节点，可复用技能系统的判定组件）
            ↓  命中查询：Physics.Overlap* / SphereCast*  + 单一来源的 LayerMask
受击方 hurtbox（身上的碰撞体，显式声明为"可被打"）
  ├─ 玩家：Collider + 结果解析（格挡 / 弹反 / 无敌帧）
  └─ 敌人：Collider + 结果解析（僵直 / 倒地 / 无敌）
            ↓  结果解析：统一走 HitResolver + IHitReactable
结算与反馈（伤害 / 音效 / 顿帧 / 击退 / 飘字）
  去重：同一次挥击对同一目标只结算一次
```

---

## 六、整合方案（三档，按投入排序）

**方案 A：只文档化 + 命名规整（0 代码改动，约 2 小时）**
- 产出：本清单文档 + 论文里的判定分层图。
- 动作：把 Inspector 上的字段按链路改名分组（如 `hitbox_*` / `hurtbox_*` / `ai_*`），只改名字不改逻辑。
- 收益：答辩时你能指着图说清"我有四类判定、分别在哪"，不再是一团浆糊。
- 风险：几乎为零（改名要同步 Inspector 里的引用）。

**方案 B：抽公共判定工具 + 单一 LayerMask 来源（约 1 天，性价比最高）**
- 新建 `HitQuery`（静态工具）：输入"判定体积 + 掩码 + 排除列表"，统一返回去重后的命中结果；
- 普通连击、技能、弹反、敌人攻击四处全部改走它；
- LayerMask 收进一个 `CombatLayers` 资产（或一个静态配置类），消除"注释维系一致"的隐患；
- 敌人攻击也给它一个判定体积（手/爪节点），不再用"距离阈值"。
- 收益：四套口径收敛成一套；论文能写"统一命中查询层"；答辩现场好讲。
- 代价：动 4 个文件，需要重新验证手感（半天测试）。

**方案 C：完整 hitbox/hurtbox + 接口化（2~3 天，答辩前不建议）**
- 玩家与敌人都实现 `IHitReactable`，`HitResolver` 成为唯一结果解析入口；
- 受击盒/攻击盒都在编辑器里可视化与配置；
- 收益：论文可写"统一的判定框架 + 对照实验"，技术含量最高。
- 代价：改动面大，答辩前风险高，建议写进"展望"。

**推荐节奏**：**A 现在就做**（今天/明天），**B 排在 10 月上旬**（跟论文第三、四章同步写），**C 写进论文展望**。

---

## 七、论文里怎么用这份清单

- **第三章 系统分析**：用第一节的分层图说明"攻击判定 / 受击判定 / 感知判定 / 手感判定"四类，并说明为什么要分开。
- **第五章 系统实现 · 关键问题解决**：写"判定口径不统一"这个问题，以及 B 方案怎么收敛（改前 4 套 → 改后 1 套查询 + 1 套解析）。
- **第六章 系统测试**：用 `_useWeaponHitbox` 开关、`_useSurfaceHitCheck` 开关做对照实验（旧口径 vs 新口径），观测"2 米外 / 贴近 / 侧面 / 高速连击"四种场景。

---

## 八、统一方案设计稿（攻击判定 + 受击判定都由编辑器配）

> 目标（用户诉求）：把攻击判定与受击判定统一成一套框架，且**两者都能在编辑器里配**；技能编辑器先支持，后续的连招编辑器能让"不同连招段使用不同的判定框与受击框"。
> 本节只是设计，不含代码改动。落地按"阶段"分步，每步都能单独验收。

### 8.1 数据模型（判定的唯一真相）

两个可序列化数据类，都是"形状 + 相对骨骼的摆放 + 生效条件"：

```
HitboxData（攻击盒，属于"动作/技能/连招段"）
  ├─ 形状：Sphere / Capsule / Box
  ├─ 尺寸：radius / capsuleLength / boxHalfExtents
  ├─ 摆放：绑定节点名（骨骼名或路径）+ offset + rotationOffset
  ├─ 时间：startFrame / endFrame（生效帧区间）
  ├─ 筛选：targetMask（打谁）
  └─ 结果：damage / poiseDamage / 击退 / 音效前缀 / 顿帧参数（可选，也可继续用现有的伤害/反馈组件）

HurtboxData（受击盒，属于"角色"）
  ├─ 形状：Sphere / Capsule / Box
  ├─ 尺寸：radius / capsuleLength / boxHalfExtents
  ├─ 摆放：绑定节点名（如头/躯干/手）+ offset + rotationOffset
  └─ 条件：是否可被击、倍率（如"打头 1.5 倍伤害"）、生效姿态（可选）
```

要点：**两个数据都是"相对骨骼"的**——角色转身/缩放/换模型都不影响；这正是现在技能系统 `attachNodeName + offset + rotationOffset` 的思路，只是把它复制到受击方。

### 8.2 运行时结构（一套查询 + 一套解析）

```
攻击方（技能组件 HitboxComponent，替换现有 HitWindowComponent 的判定部分）
  ├─ 判定帧内逐帧：按绑定节点取世界变换
  ├─ 支持一个动作挂多个 HitboxData（剑尖/剑身/脚，各自帧区间）
  └─ 调用 HitQuery（统一查询）
        ↓
HitQuery（静态工具，唯一查询入口）
  ├─ Physics.OverlapCapsule / OverlapBox / OverlapSphere（体积）
  ├─ Physics.SphereCastAll（上一帧 → 这一帧的扫掠，防高速漏判）
  ├─ 单一来源的 LayerMask（收进一个 CombatLayers 配置）
  └─ 同一次挥击对同一目标只返回一次（去重）
        ↓
HurtboxReceiver（挂在玩家/敌人身上，受击方的唯一入口）
  ├─ 持有若干 HurtboxData（默认一个躯干盒，可加头/手/脚）
  ├─ 实现 IHitReactable：IsBlocking / IsParryWindow / IsInvincible
  ├─ 决定命中结果：普通命中 / 格挡 / 弹反 / 无敌（统一走 HitResolver）
  └─ 结算伤害 + 反馈（音效 / 顿帧 / 击退 / 飘字）
```

这样"攻击盒属于动作、受击盒属于角色"就分清了：**换连招只改攻击盒，换角色只改受击盒**。

### 8.3 编辑器形态

| 工具 | 配什么 | 现状 | 需要补 |
|---|---|---|---|
| **技能编辑器**（已有） | 攻击盒：形状/尺寸/绑定节点/帧区间 + Scene 判定盒线框与手柄 | 已能配 1 个判定盒（`HitWindowComponent`） | 支持**多个判定盒列表**；把判定盒数据抽成 `HitboxData`；受击盒倍率等结果参数 |
| **受击盒编辑器**（新增） | 受击盒：在角色身上摆若干盒（头/躯干/手/脚），画线框 + 拖手柄 | 无 | 一个 `HurtboxReceiver` 的 CustomEditor + Scene 可视化（可参考技能编辑器里判定盒手柄的写法） |
| **连招编辑器**（后续） | 时间轴 + 多段：每段引用自己的攻击盒列表；每段可覆盖受击盒（霸体/无敌姿态） | 无 | 时间轴 + 段列表 + 每段判定盒预览（工作量最大） |

### 8.4 落地阶段（每步可单独验收）

**阶段 1：运行时统一（约 1~1.5 天）**
- 新增 `HitboxData` / `HurtboxData` / `HitQuery` / `HurtboxReceiver`；
- 玩家普攻、玩家技能、敌人攻击三处判定改走 `HitQuery`；命中结果统一走 `HurtboxReceiver`；
- `Enemy` 与玩家受击逻辑迁移到 `HurtboxReceiver`（保留原有无敌帧、僵直、击退行为）；
- 验收：三种攻击手感不变；`同一目标不再被两套逻辑叠加结算`；Console 能打出"命中 → 结果 → 结算"的统一日志。

**阶段 2：编辑器可配（约 1 天）**
- 技能编辑器：判定盒从"单盒字段"升级为"**多个 HitboxData 的列表**"，每个盒独立帧区间 + Scene 线框/手柄；
- 新增受击盒编辑器：在角色身上摆受击盒，Scene 里可视化；
- 验收：编辑器里能给同一技能配 2~3 个判定盒（剑尖/剑身/脚），能调整敌人的受击盒大小与位置，改完运行时立即生效。

**阶段 3：连招编辑器（约 2~3 天，建议排到 10 月或写进展望）**
- 时间轴 + 多段：每段引用自己的攻击盒列表；
- 每段可覆盖受击盒（例如某段带霸体、某段起手有短无敌）；
- 验收：不同连招段使用不同判定框与受击框，且能在编辑器里逐帧预览。

### 8.5 取舍与风险

- **收益**：判定从"四套口径"变成"一套框架"；攻击盒与受击盒都能在编辑器里配——这正好对应任务书"可视化配置连招转换规则、攻击判定参数与动画事件"；答辩时能直接对着商业做法讲。
- **风险**：阶段 1 会改动三个已有链路（普攻/技能/敌人攻击）的结算路径，**必须逐个回归**（伤害数值、顿帧、击退、无敌帧、僵直、飘字）；建议保留旧的开关（如 `_useWeaponHitbox`）作为对照，便于回滚与做论文对照实验。
- **不建议**：不要在这一版引入 CCD/关节物理；判定精度靠"体积 + 帧窗口 + 扫掠"已经够。
- **时间提醒**：阶段 1+2 约 2~2.5 天（按每周 5 小时约 3 周），落在 10 月上旬比较合适；阶段 3 如果影响论文进度就写进"展望"。

---

## 九、阶段 1 改动清单（待确认后动手）

> 已确认的前提：① 按"阶段 1 → 阶段 2"顺序；② 受击盒**先不开新 Layer**，用"现有 Enemy/Player 层 + `HurtboxReceiver` 组件过滤"；③ 旧开关（`_useWeaponHitbox` / `_useSurfaceHitCheck`）**保留**，既做回滚保险也做论文对照。
> 阶段 1 只做"运行时统一"，不动编辑器界面（编辑器留到阶段 2）。

### 9.1 拆成两小步，先保行为、再收查询

**1a：统一"结算与结果解析"（风险最低，先做）**

| 动作 | 文件 | 说明 |
|---|---|---|
| 新增 | `Assets/Scripts/Combat/HitInfo.cs` | 一次命中的数据结构：`damage / hitPoint / attacker / direction` |
| 新增 | `Assets/Scripts/Combat/HurtboxReceiver.cs` | 受击方的**唯一入口**，挂玩家与敌人；持 `damageScale`（阶段 2 可配"打头 1.5 倍"） |
| 新增（可选） | `Assets/Scripts/Combat/CombatInterfaces.cs` | 把 `IHitReactable` / `HitResolver` / `HitOutcome` 从 `HitWindowComponent.cs` 搬过来（纯搬家，语义不变），让"判定框架"与"技能组件"解耦 |
| 改 | `EnemyAI.cs` | 实现 `IHitReactable`（阶段 1 先返回 `false`，行为与现在一致；阶段 2 再填真实格挡/弹反） |
| 改 | `PlayerCombat.cs` | 实现 `IHitReactable`（`IsBlocking` = 格挡状态；`IsParryWindow` = 弹反窗口）→ 这样技能打玩家时格挡/弹反终于能生效 |
| 改 | `HitWindowComponent.ApplyHit` | 不再直接 `Enemy.TakeDamage`，改为 `HurtboxReceiver.ReceiveHit(...)` |
| 改 | `PlayerCombat.PerformHitDetection` | 同上，改走 `HurtboxReceiver` |
| 改 | `EnemyAI.TryDealDamage` | 同上（查询方式先不动，仍用表面距离） |

**1b：统一"命中查询"（紧接着做）**

| 动作 | 文件 | 说明 |
|---|---|---|
| 新增 | `Assets/Scripts/Combat/HitQuery.cs` | 唯一查询入口：`OverlapSphere / OverlapCapsule / OverlapBox`（体积）+ `SweepSphere`（上一帧→这一帧的扫掠）+ 组件过滤（只认 `HurtboxReceiver`）+ 去重；内部用 `NonAlloc` 复用缓冲，不产生 GC |
| 改 | `PlayerCombat.PerformHitDetection` | 用 `HitQuery.OverlapCapsule` + `HitQuery.SweepSphere` 替换手写的两条查询 |
| 改 | `HitWindowComponent.DetectHits` | 用 `HitQuery` 替换它内部的球/胶囊/盒查询（保留三采样扫掠逻辑，只是把"查"这步收进工具） |
| 改（可选，需调参） | `EnemyAI.TryDealDamage` | 给敌人攻击一个判定体积（手/爪节点 + `HitQuery.OverlapSphere`），彻底告别"表面距离"。**这一步会改变敌人攻击手感，需要重新调半径**，可以放到阶段 2 跟编辑器一起做 |

### 9.2 `HurtboxReceiver` 怎么接管现有的无敌帧 / 格挡 / 僵直

保持**行为完全不变**的原则：`HurtboxReceiver` 只做"结果解析 + 转发结算"，具体效果仍由原来的代码负责。

```
命中 → HurtboxReceiver.ReceiveHit(damage, point, attacker)
        ├─ 1) if (attacker 是自己人) return false;                      // 防止自己打自己
        ├─ 2) 结果解析：HitResolver.Resolve(transform, attacker)
        │        ├─ Invincible → return false（不结算、不播反馈）
        │        ├─ Blocked   → damage × blockedDamageScale
        │        └─ Parried   → damage = 0
        ├─ 3) 转发结算：
        │        ├─ 敌人：Enemy.TakeDamage(damage, dir)      // 僵直累加、无敌帧、击退、死亡全在 Enemy 内，不变
        │        └─ 玩家：ThirdPersonController.TryTakeDamage(damage, attackerPos)  // isInvulnerable 拦截、受击动画、飘字，不变
        └─ 4) return true（调用方据此播音效/顿帧/飘字）
```

去重放在**攻击方**：同一次挥击维护一个"已命中列表"（玩家普攻用现有的 `hitEnemies`，技能用现有的 `hitTargets`，敌人攻击新增一个），命中过的 `HurtboxReceiver` 直接跳过。

### 9.3 回归清单（改完逐条勾，约 15 分钟）

阶段 1 动了三条链路的结算路径，**必须逐条验证没改坏**：

- [ ] 玩家左键连击三段：每段都有**伤害数字**、**命中音效**、**顿帧**
- [ ] 玩家连击命中敌人：敌人有**击退**、僵直条增长、满僵直后进入僵直/倒地
- [ ] 玩家右键放技能（`player_uppercut`）：能命中、掉血、反馈正常
- [ ] 技能打空（怪在 2 米外）：不命中、不播音效、不顿帧
- [ ] 敌人攻击玩家：玩家掉血、有受击动画、被击退
- [ ] 玩家闪避 / 格挡期间被敌人打：**不扣血**（`isInvulnerable` 仍生效）
- [ ] 玩家弹反：敌人进入受击/僵直，玩家不扣血
- [ ] 敌人被弹反时的 30 点僵直伤害仍生效（`EnemyAI.OnParried`）
- [ ] 敌人被打死：死亡动画 + 结算面板正常
- [ ] 敌人无敌帧仍生效（同一刀砍中同一次不会连续扣两次血）
- [ ] Console 里能看到"命中 → 结果 → 结算"的统一日志（新增，便于以后排查）

### 9.4 验证顺序建议

1. 先做 **1a** → 跑一遍上面 11 条回归清单（不改查询，风险最小）；
2. 再做 **1b** → 再跑一遍清单，重点看"高速连击是否漏判""2 米外是否误判"；
3. 都通过后，编辑器侧（阶段 2）才动界面。

如果 1a 或 1b 中途出现手感异常，**旧开关就是回滚按钮**：`_useWeaponHitbox` 取消勾选回到旧的球形判定，`_useSurfaceHitCheck` 取消勾选回到敌人的中心点距离判定。

---

## 十、能不能"四套合成一套"？——能，但要这样理解

### 10.1 统一后：1 套框架 + 3 个触发点 + 1 个防御状态

```
【触发点】谁在什么时候开判定（这部分天然不同，保留 3 个）
  ① 玩家普通连击 → 动画事件（挥击帧，一次）
  ② 玩家技能     → 帧窗口（逐帧，内部按帧去重）
  ③ 敌人攻击     → 挥击帧（一次）

【判定体积】全部是同一种数据 hitbox（形状 + 尺寸 + 绑定骨骼 + 偏移/旋转 + 帧区间）
        ↓
【查询】唯一入口 HitQuery
  OverlapSphere / OverlapCapsule / OverlapBox（体积）
  + SweepSphere（上一帧 → 这一帧的扫掠）
  + 单一 LayerMask + 同一次挥击去重
        ↓
【受击】唯一入口 HurtboxReceiver（挂在玩家/敌人身上，持受击盒数据）
        ↓
【解析】唯一入口 HitResolver + IHitReactable
  普通命中 / 格挡 / 弹反 / 无敌
        ↓
【结算与反馈】伤害 / 音效 / 顿帧 / 击退 / 飘字
```

**"防御状态"**指弹反：它不该算作第四套攻击判定，而是**受击方在弹反窗口内的一种解析结果**——敌人打过来时，玩家的 `HurtboxReceiver` 把这次命中解析成 `Parried`，于是敌人进入被弹反状态。这样"四套"就变成了 **3 个触发点 + 1 套查询 + 1 套受击 + 1 套解析**。

### 10.2 统一前后对比（同一件事的两种写法）

**现在（四处各写一遍）**
```csharp
// 普攻：自己拟合胶囊 + 两条查询 + 找 Enemy + TakeDamage + 飘字
// 技能：自己算节点 + 三采样扫掠 + 找 Enemy + HitResolver + TakeDamage + 反馈
// 弹反：自己 OverlapSphere + 找 EnemyAI + OnParried
// 敌人攻击：自己算表面距离 + TryTakeDamage
```

**统一后（每个触发点只有几行，且长一个样）**
```csharp
// 玩家普攻（动画事件）
var hits = HitQuery.OverlapCapsule(node, hitbox, enemyMask, _swingHits);   // 体积
HitQuery.SweepSphere(lastCenter, center, hitbox, enemyMask, _swingHits);  // 扫掠
foreach (var hb in hits) hb.ReceiveHit(damage, point, transform);         // 受击方解析+结算

// 玩家技能（帧窗口内）
foreach (var box in hitboxes)
    if (box.IsActiveFrame(currentFrame))
        HitQuery.OverlapCapsule(box, enemyMask, _swingHits);

// 敌人攻击（挥击帧）
HitQuery.OverlapSphere(handNode.position, hitbox.radius, playerMask, _swingHits);

// 弹反：不需要"第四套"了 —— 玩家在弹反窗口内时，上面的 ReceiveHit 会解析成 Parried
```

### 10.3 统一能带来什么 / 不会带来什么

- **会带来**：判定只有一个入口、一种写法；答辩时一句话讲清（"所有攻击都是判定盒 → 查受击盒 → 受击方决定结果，区别只在盒子的形状、时间窗和绑定骨骼，而这些都在编辑器里配"）；论文能画一张框架图 + 做对照实验。
- **不会带来**：代码量不会大幅减少（触发时机不同就得分开写）；每个动作的判定盒尺寸/帧区间仍然不同 —— **但那属于"数据差异"，不是"代码差异"**，这正是"编辑器可配"的意义。

### 10.4 一个需要你拍板的设计变更

**弹反要不要改成"被动解析"？**

- **现在**：按弹反键 → 以角色为中心球扫描周围 2 米 → 命中范围内的**所有**敌人一律弹反（不管它有没有在攻击你）。
- **统一后（建议）**：按弹反键只是**打开弹反窗口**；只有当敌人真的打到你、且你处于窗口中时，这次命中才被解析成 `Parried`，敌人进入被弹反状态。
- **影响**：更符合动作游戏的常识（"看好时机弹开打过来的一刀"），也不再需要那套中心球扫描；但**会改变玩法手感**——以前可以无差别弹开附近敌人，改后必须等敌人出手。
- 决定：**【A】改成被动解析（推荐，判定更统一）** / **【B】保留现在的主动扫描**（写作量最小，但弹反仍是"第四套判定"）。
