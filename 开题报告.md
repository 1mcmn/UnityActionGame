# 开题报告：基于 Unity 的动作游戏与编辑器工具的设计与实现

| 项目 | 内容 |
|---|---|
| 课题名称 | 基于 Unity 的动作游戏与编辑器工具的设计与实现 |
| 学生 | 邬锦飞 |
| 指导教师 | 王欣 |
| 专业班级 | 软件工程（专升本）一班 |

> 说明：题目保持不变（经指导教师确认）。本版报告按开题答辩意见对本课题的技术主线与内容重心做了调整——**以 Unity 编辑器工具为研究主线，以动作游戏原型作为工具能力的验证平台**；修改对照见文末"附：根据开题答辩意见的修改说明"。

---

## 一、课题背景与意义

动作游戏是游戏市场中最具代表性的品类之一，其核心玩法围绕实时战斗、角色操控与敌人 AI 展开。动作游戏的战斗体验高度依赖精细的参数调校（连击窗口、伤害数值、判定时机、帧级音效同步等）与高效的开发工具支撑。然而在实际开发中，这些战斗参数通常硬编码在脚本中或分散于多个 Inspector 面板：修改一处手感参数往往需要改代码、等待编译、重新运行，非程序人员（策划、美术）无法参与调优，且缺乏校验手段——参数写错只能在运行时暴露。

本课题以 **Unity 编辑器扩展**为技术主线，面向动作游戏设计并实现一套**可视化战斗配置工具链**：把战斗参数与帧级时序从代码中剥离为可编辑、可校验的资产，让内容生产脱离代码。为使工具的配置化能力可被客观检验，同时实现一个可运行的第三人称动作游戏原型作为**验证平台（testbed）**——游戏在本课题中的角色是"证明工具有效"的载体，而非研究对象本身。

课题的工程意义在于：将主观的战斗手感拆解为可度量、可配置的参数，并通过编辑器工具实现"编辑配置 → 资产 → 运行时即时生效"的闭环；同时把配置错误从运行期前移到编辑期，降低协作成本与调试成本。

## 二、国内外研究现状（文献待补，检索方向已明确）

本课题的相关研究可归为三类，本次按"现状 → 不足"梳理：

1. **游戏引擎架构与动作游戏战斗系统设计**：状态机驱动角色行为、连击窗口与输入缓冲、判定帧窗口（hitbox/hurtbox）、受击与硬直系统等已成为通用工程模式；
2. **数据驱动游戏开发与可视化配置**：以资产（Asset）承载配置、以可视化脚本/可视化编辑替代硬编码，是引擎工具化的重要方向，代表形态包括节点式可视化脚本、配置表驱动与资产驱动；
3. **引擎编辑器扩展与工具链开发**：基于引擎宿主 API 的编辑器扩展（自定义窗口、Inspector 扩展、资产管线）是内容生产效率的关键环节。

**存在的不足（本课题的切入点）**：现有方案多为面向大型团队与全平台的通用工具，配置项粒度粗、学习成本高；而**面向动作游戏帧级战斗时序（判定窗口、帧事件音效、无敌帧区间）的可视化配置与一致性校验**的工具化方法与可复现设计，在中小规模项目中研究较少，多数项目仍以代码硬编码 + 手工调参的方式完成。

> 注：以上为研究方向的框架性描述，具体文献与引用格式需按学校要求补充真实来源（待补）。
> 建议检索方向（需自行核实并补入，勿直接照抄）：`Unity 编辑器扩展 / 引擎工具链`、`数据驱动游戏开发 / 可视化脚本`、`动作游戏战斗系统 手感 / 打击感`、`技能编辑器 / 配置工具 / 可视化配置`、`行为状态机 游戏 AI`、`DSL / 领域特定语言 游戏配置`。

## 三、研究目标与主要内容

**总目标**：设计并实现面向动作游戏的 Unity 可视化战斗配置工具链，使战斗内容的生产脱离代码，并以可运行的验证平台检验其有效性。

1. **编辑器工具（研究主线）**：
   - **技能编辑器**（EditorWindow）：技能与组件列表配置、组件参数与命名、帧事件表格（帧号 / 音效 ID / 音量 / 编辑器内试听）、动画预览；
   - **音效管理器**（CustomEditor 扩展 SoundLibrary）：soundID ↔ AudioClip 的表格化编辑、批量导入、编辑器内试听；
   - **配置正确性校验（拟实现）**：在编辑期检查引用完整性、帧号越界、音效 ID 缺失、参数非法与 skillId 重复，把错误从运行期前移到编辑期；
2. **数据层**：编辑器工具与运行时通过 ScriptableObject 资产解耦，实现"编辑器修改、游戏即时生效"；组件以 `[SerializeReference]` 多态内嵌，支持装配式扩展；
3. **验证平台（支撑，非研究重点）**：实现第三人称动作游戏原型（角色操控、怪物 AI、连击/弹反/格挡、顿帧、伤害数字、音效与胜负结算），用于验证工具的配置化能力与配置结果的正确性。

## 四、总体架构与技术路线（重点）

### 4.1 总体三层架构

```
┌──────────────────────────────────────────────────────────┐
│  编辑器层（Assembly-CSharp-Editor，仅编辑器运行）          │
│  技能编辑器（EditorWindow） / 音效管理器（CustomEditor）    │
│  技术：EditorWindow + SerializedObject + ReorderableList   │
│       + AssetDatabase（IMGUI）                             │
│  [拟扩展] 配置正确性校验：引用/帧号/音效ID/参数/唯一性      │
├──────────────────────────────────────────────────────────┤
│  数据层（ScriptableObject 资产，唯一的"数据库"）            │
│  SkillLibrary / SkillConfig（组件内嵌 [SerializeReference]）│
│  / ComboData / SoundLibrary / EnemyConfig                  │
├──────────────────────────────────────────────────────────┤
│  运行时层（验证平台，只读数据层）                           │
│  技能系统 SkillManager / 角色状态机 / 敌人 FSM             │
│  / 音效 SoundManager / 伤害飘字对象池                       │
└──────────────────────────────────────────────────────────┘
层间通信：编辑器写资产 → 运行时读资产（无直接调用，天然解耦）
```

### 4.2 编辑器工具架构（研究主线）

| 工具 | 形态与功能 | 数据载体 |
|---|---|---|
| 技能编辑器 | EditorWindow 多面板：技能 ID/名称、组件列表增删排序（`managedReferenceValue`）、组件参数与命名、帧事件表格（音效@帧、编辑器内试听）、动画预览 | SkillLibrary / SkillConfig |
| 音效管理器 | CustomEditor 扩展 SoundLibrary Inspector：soundID ↔ clip 表格化编辑、文件夹批量导入、编辑器内试听 | SoundLibrary |
| 配置校验（拟实现） | 编辑期静态校验：引用完整性、帧号越界（帧号 > 剪辑长度）、音效 ID 不存在、参数非法、skillId 重复 | 校验结果在工具窗口内以列表呈现 |

共同机制：SerializedObject 数据绑定 → ApplyModifiedProperties → SetDirty 保存 → 运行时直接读资产，实现即时生效。

**工具形态的选择依据**：技能编辑器配置对象多、面板层次深（技能 → 组件 → 参数 → 帧事件），采用独立 EditorWindow 以获得完整的布局控制；音效管理器是单表结构，采用 CustomEditor 内嵌 Inspector 更轻量、更贴近 Unity 原生操作习惯。

### 4.3 技能系统架构（工具的核心数据模型）

```
SkillLibrary（技能 ID 索引表）
   └─ SkillConfig（技能资产：skillId + 组件列表）
        └─ List<SkillComponent>（[SerializeReference] 多态内嵌）
             ├─ PlayAnimationComponent（播动画 + 帧事件派发）
             ├─ HitWindowComponent（判定窗口：球/胶囊/盒三形状）
             │     ├─ 引用 DamageComponent（伤害数值）
             │     └─ 引用 HitFeedbackComponent（音效+顿帧）
             └─ InvincibleWindowComponent（无敌帧窗口）

运行时：SkillManager（单例）.PlaySkill(id, caster, target)
   → SkillInstance（生命周期：None→Released→Running→Ended/Interrupted）
   → 组件四态：OnStart / OnTick / OnEnd / OnInterrupt（打断必清理：判定区、无敌帧零残留）
```

设计要点：
- **组件模式**：技能 = ID + 组件列表，组合优于继承；新增技能 = 添加组件配置，不改代码；
- **帧级驱动**：帧事件（音效@帧）与判定窗口（伤害@帧区间）以动画帧为时间基准，保证"动画、判定、音效"三者同步——解决动作游戏中判定与表现不一致的经典问题；
- **生命周期闭环**：任何时刻打断技能，系统立即中断并执行清理，不残留判定区/无敌帧污染后续行为；
- **内嵌序列化**：组件用 `[SerializeReference]` 内嵌于技能资产，编辑器中直接添加、命名、调参，零资产文件操作（策划友好）。

### 4.4 验证平台：游戏运行侧架构（支撑工具验证）

| 模块 | 采用技术 | 说明 |
|---|---|---|
| 角色控制 | 状态机模式（玩家 10 态：Idle/Move/Run/Dodge/Attack/Parry/Block/Hit/Knockdown/Dead） | 行为切换集中管理，配合输入缓冲（0.18s 预输入窗口）提升手感 |
| 怪物 AI | 13 态 FSM（Spawn→Idle→Walk→RunStart→Run→RunEnd→Attack(7 段)→Staggered→Stun*→Death） | 基于距离决策 + 连段递进与冷却 |
| 动画系统 | Animator + Blend Tree（Idle/Walk/Run）+ Trigger 驱动；**Root Motion**（OnAnimatorMove 驱动位移） | 攻击/闪避由动画驱动位移，保证表现与位移一致 |
| 物理 | Rigidbody（MovePosition/MoveRotation、OverlapSphere/OverlapCapsule/OverlapBox 判定） | 任务书指定方案 |
| 打击反馈 | 顿帧（Time.timeScale 短时缩放）、伤害数字（对象池）、音效分层（前缀随机播放、动态 Pitch） | 打击感三件套 |
| 数据解耦 | ScriptableObject 资产优先、内联字段回退 | 资产未赋值时游戏仍可运行 |

> 说明：本模块的作用是为工具提供真实的使用场景与验证对象（配置是否生效、配置错误能否被提前发现、配置化是否覆盖战斗内容生产）。

### 4.5 关键技术选型与对比（答辩素材）

| 决策点 | 选择 | 对比方案与理由 |
|---|---|---|
| 编辑器 UI | IMGUI（EditorGUILayout） | UI Toolkit（UXML/USS 学习曲线陡）；IMGUI 与任务书技术栈一致、API 简洁 |
| 工具形态 | EditorWindow（技能编辑器）+ CustomEditor（音效管理器） | 全用 CustomEditor 难以承载多面板层次；全用独立窗口对单表结构过重 |
| 数据存储 | ScriptableObject | 硬编码（不可配）/ JSON（需加载路径管理）；SO 资产化 + Inspector 可视化 |
| 组件序列化 | `[SerializeReference]` 内嵌 | 独立 SO 资产引用（多次创建文件、策划负担重）；内嵌后界面直接调参 |
| 运行时通信 | 单例 + C# 事件（观察者） | 全局事件总线（字符串键、难调试）/ DI 框架（超纲）；项目规模小，单例+事件足够 |
| 判定时机 | 动画帧窗口驱动 | 固定延迟（表现与判定脱节）；帧窗口保证与动画严格同步 |
| 动画系统 | Animator 状态机 | Animancer（付费、任务书指定 Animator）；Blend Tree + Trigger 满足需求 |

### 4.6 设计模式清单

组件模式（技能组装）、状态模式（角色/敌人 FSM）、单例模式（SoundManager/SkillManager/GameManager）、观察者模式（C# 事件广播）、对象池模式（音效/飘字）、数据驱动（资产中介解耦）。

## 五、关键技术难点与解决方案

| 难点 | 解决方案 | 状态 |
|---|---|---|
| 动画与判定不一致（动作游戏核心痛点） | 帧事件 + 判定帧窗口，以动画归一化时间驱动判定与音效 | 已实现 |
| 技能打断的残留污染 | 四态生命周期（OnInterrupt 强制清理判定区/无敌帧），并以 EditMode 单元测试覆盖生命周期与打断清理 | 已实现，测试计划扩容 |
| 连击手感（连点抽搐/慢点连不上） | 动画进度驱动的连击窗口 + 残响（grace）窗口 + 窗口外输入忽视 | 已实现，持续调参 |
| 组件之间的引用语义 | 判定窗口对伤害/反馈组件的引用必须保持引用语义（否则被按值序列化产生副本），需在序列化与编辑器绑定层给出统一方案 | 待改进 |
| 配置错误只能在运行时暴露 | 编辑器侧静态校验（引用完整性、帧号越界、音效 ID 缺失、参数非法、skillId 重复），错误前移到编辑期 | 拟实现 |
| 编辑器修改即时生效 | 三层架构 + ScriptableObject 资产中介，编辑器与运行时零直接依赖 | 已实现 |
| 组件配置的策划易用性 | `[SerializeReference]` 内嵌组件，界面内添加/命名/调参 | 已实现 |

## 六、进度安排

| 阶段 | 时间 | 工作内容 |
|---|---|---|
| 调研与开题 | 2026.07 – 2026.09 | 调研动作游戏战斗系统与 Unity 编辑器扩展 API；搭建验证平台最小可玩闭环；完成开题报告与开题答辩 |
| 工具深化 | 2026.09 下旬 – 2026.10 上旬 | 完善技能编辑器（组件配置、帧事件、动画预览）与音效管理器；设计并实现配置正确性校验 |
| 验证与数据采集 | 2026.10 中旬 – 2026.11 上旬 | 配置化覆盖率与配置效率对比、时序一致性测量、边界场景测试；EditMode 单元测试扩容 |
| 论文撰写 | 2026.11 上旬 – 2026.11 下旬 | 完成论文初稿（以编辑器工具为主线，游戏作为验证平台） |
| 修改与答辩准备 | 2026.11 下旬 – 2026.12 | 按评阅意见修改论文；录制系统演示视频；答辩演练 |

## 七、预期成果

1. **Unity 可视化战斗配置工具链**：技能编辑器（组件装配、帧事件、动画预览、配置校验）+ 音效管理器；
2. **验证平台**：可运行的第三人称动作游戏原型（角色操控、怪物 AI、完整战斗循环），用于验证配置化能力与配置正确性；
3. **验证数据与测试**：配置效率对比、时序一致性测量、EditMode 单元测试；
4. **毕业论文一篇**；
5. **系统演示视频（5–10 分钟）**。

## 八、参考文献（待补真实文献）

- Unity 官方文档：EditorWindow、ScriptableObject、SerializeReference、Animator；
- 《游戏编程模式》（Robert Nystrom）：状态模式、组件模式；
- 动作游戏战斗系统 / 打击感设计相关文献（待补，需含期刊或学位论文）；
- 数据驱动游戏开发 / 可视化脚本相关文献（待补）；
- 引擎编辑器扩展与工具链相关文献（待补，建议含 1–2 篇外文）。

> 注：本部分必须补充真实可检索的文献与规范引用格式，检索方向见第二节。

---

## 附：根据开题答辩意见的修改说明

| 开题答辩意见 | 本次处理 | 涉及章节 |
|---|---|---|
| 内容偏杂、重点不突出 | **题目保持不变**；内容重心收敛为编辑器工具，游戏部分明确定位为"验证平台"，删去"融合游戏玩法开发与编辑器扩展两大技术方向"的双中心表述 | 一、三、四、七 |
| 不清楚具体做了什么 | 明确研究主线为技能编辑器 + 音效管理器两个工具，并补充"配置正确性校验"作为可辨识的研究点 | 三、4.2 |
| （前期调整）工具数量 | 怪物生成器经指导教师同意取消，报告与预期成果同步改为两个工具 | 三、4.1、4.2、七 |
| 报告与实际不一致 | 修正"运行时自测清单"等无对应产物的表述，改为可核实的 EditMode 单元测试 | 五 |
| 进度与主线不符 | 进度表按真实时间重排，验证平台在前、工具深化与验证数据在后 | 六 |
