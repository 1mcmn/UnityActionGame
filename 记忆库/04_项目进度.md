# 04 项目进度

> 当前状态 + 下一步。**新会话先读这里 + `答辩冲刺计划.md` 顶部"当前状态"区。**

## 当前（2026-08-21 晚）

**✅ 已完成**
- 游戏本体闭环逻辑全通（受击/伤害/僵直/伤害数字/对象池/结算）
- 14 个 EditMode 测试全绿
- 音效工具：波形/裁剪/保存（AudioBrowser，WAV 头验证正确）
- 音效条目管理器已升级为编辑 SoundLibrary（正式音效管理器雏形）
- 记忆库搭建完成（本文件体系）
- AGENTS.md（自动加载的项目说明书）

**❌ 待办（按优先级）**
1. **明天上午（8/22）**：修敌人动画——① 7 个 FBX clip 改名（Mutant Punch→Goblin_Ani_Attack_01、Stand Up→Goblin_Ani_Born、Stunned→Goblin_Ani_Debuff_Stun_Loop、Dying→Goblin_Ani_Death_Hit_Front、Medium Run→Goblin_Ani_Run、Mutant Walking→Goblin_Ani_Walk、Orc Idle→Goblin_Ani_Idle）② Monsterctrol.controller 空状态绑动画 + 补缺状态 + 删重复 "Death_Hit_Front 0"
2. **30 秒**：场景 SoundManager 的 Libraries 挂 PlayerSounds.asset
3. **8/24 起**：动作编辑器（ComboData/ActionData 扩展 + 帧事件 + 表格界面）
4. **10 月**：怪物生成器/关卡编辑器（待导师确认）+ UI 主题
5. **11 月**：论文/演示视频/PPT

**⏸ 暂停**：OpenViking 记忆插件（WSL 403，见 `答辩后实验清单.md`）

## 开题报告要求（2026-09 补录）

- **学校**：杭州电子科技大学信息工程学院；**学号** 259400232；指导教师 王欣；专业 软件工程（专升本）
- **结构要求（5 节，顺序固定）**：① 综述国内外研究动态 + 选题依据与意义（1.1 国内 / 1.2 国外 / 1.3 依据与意义）② 研究基本内容 + 拟解决的主要问题（2.1 基本内容含**功能模块图** / 2.2 主要问题：功能结构设计、框架搭建、数据库设计、安全性、功能性测试、其他关键技术问题）③ 研究步骤、方法及措施 ④ 研究工作进度（**与任务书一致**）⑤ 主要参考文献（**GB/T 7714** 格式）
- **排版**：正文小四号宋体；页边距 左 3cm、右 2.5cm、上下各 2.5cm；行距固定值 20 磅；A4；全文编号方式一致
- **字数**：正文 **≥ 3000 字**
- **文件位置**：`C:\Users\admin\Downloads\1a 开题报告要求及模版.doc`（内含一份 **3447 字**的合规草稿，结构完全按学校模板；4/5 两节为空）
- **我方产出**：`开题报告.md`（8 节自拟结构，作**素材库**用）；提取的纯文本在 `_提取文本\`（含 `.doc` 表结构丢失、尾部乱码，需人工核对）

## 体检发现（2026-09-19，全面排查后）

**编译**：`compile_check.bat` 全绿（仅 1 个 CS0108 警告，来自练习脚本 `Myobject.cs`）。

**代码问题（按危害排序，详见 `02_踩坑记录.md` 2026-09-19 段）**
1. 🔴 顿帧 `timeScale` 无引用计数 + 技能系统"每命中一次调一次" → 同帧多杀会**永久 10% 慢动作**
2. 🔴 技能结束后不恢复动画机（`PlayAnimationComponent.OnEnd/OnInterrupt` 空实现，动画状态 `AT1` 无出边）→ 角色**卡在技能最后一帧**
3. 🔴 命中音效前缀 `"hit_"` 多带下划线 → `PlayByPrefix` 永远匹配不到（每次命中刷 warning + 无声）；`block_`/`parry_` 库里不存在
4. 🟠 同一挥击可能被 `PlayerCombat` 和 `HitWindowComponent` **各判一次伤害**（双份伤害 + 双份顿帧）；`DamageComponent`/`HitFeedbackComponent` 单独放进组件列表是**空操作**（编辑数值无效）
5. 🟠 技能与玩家状态机**互不知情地抢同一个 Animator**（技能按"当前状态"算帧号，动画被抢后判定窗口错位）；技能无敌帧与闪避/格挡共用 `PlayerCombat` 一个裸 bool，互相清对方状态
6. 🟠 `TestSkillLauncher.cs`（`Assets/Scripts` 下，无 `#if UNITY_EDITOR`）会**编进正式包**，右键放技能还会和格挡冲突；全工程没有任何场景挂 `SkillManager`
7. 🟠 组件顺序是硬依赖（"播放动画"必须在"判定窗口"之前）但零校验、失败静默（判定窗口读不到帧号直接 return）= 表现为"技能完全没伤害"

**测试**：套件红着（194 通过 / 25 失败，其中 3 个我方测试过期、22 个第三方包自带测试）；批处理脚本本身因 `-quit` 静默失效——见 `02_踩坑记录.md`

**交付缺口**
- **怪物生成器 0% 实现**（任务书三大工具之一；`EnemyConfig` 数据层已就绪但无工具消费，`EditorTool_EnemySpawner` 曾在 4daf169 被删）
- 音效管理器做成了独立 EditorWindow，**任务书要求的 `[CustomEditor(typeof(SoundLibrary))]` Inspector 形式未做**；技能编辑器编辑的是 `SkillLibrary/SkillConfig`，**不是任务书要求的 ComboData 连击表格**（连击段数/每段伤害至今无编辑入口）
- `ProjectSettings/EditorBuildSettings.asset` 的 `m_Scenes: []` → **出不了包**（答辩要演示视频/构建版本时必踩）

**仓库卫生**
- 7 个文件既被 `.gitignore` 忽略又被 git 跟踪（`UserSettings/*` 5 个 + `.vs/*` 2 个），每次开工程都产生噪音 diff
- `Assets/Editor/Tool/编辑器/音效工具/` 下有 4 个 Missing Script 的孤儿资产（`AudioCreatetable.asset`/`myObject.asset`/`AudioTagData.asset` 等，GUID 断链）
- 根目录 47 个未跟踪文件里，`.doc/.docx/.pptx/.pdf` 等二进制文档和 `~$` Office 锁文件建议加 `.gitignore`；`_tools/` 里的生成脚本、`ppt-spec/`、`开题答辩PPT.md` 等属源文件建议入库
- `.meta` 完整性正常（2965 个资产↔2965 个 meta，无缺失/无孤立）

