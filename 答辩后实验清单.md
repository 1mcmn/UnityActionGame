# 答辩后实验清单（OpenViking 记忆插件）

> 状态：2026-08-21 尝试安装，卡在 WSL 安装（`wsl --install` 返回 403，网络层被拦截）。
> 已投入：Docker CLI 29.7.2 已装（未完成）；结论：**暂停，答辩后再试**。

## 它是什么
火山引擎开源的 AI Agent 上下文管理系统（文件系统范式管理记忆）。
dsh 记忆插件 = 给 DeepSeek Harness 加跨项目/跨会话长期记忆（`mcp__openviking__*` 工具 + 会话注入）。

## 当前进度
- [x] Docker CLI 29.7.2 已安装（winget）
- [ ] WSL2 —— **卡点**：`wsl --install` 报 403（微软下载端点被网络拦截，无代理可查）
- [ ] Docker Desktop 引擎启动（依赖 WSL2）
- [ ] OpenViking 服务端（`docker pull ghcr.io/volcengine/openviking`，端口 1933）
- [ ] 凭据（OPENVIKING_API_KEY 等）
- [ ] dsh 插件安装（Git Bash 里跑，不能从 cmd 跑）

## 下次再试的两条路
1. **修 WSL**：开代理/VPN 后跑 `wsl --install`；或手动 `dism /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux` + 下载 WSL2 内核 MSI
2. **免 Docker**：查一下 PyPI 的 `openviking` 包（0.4.9，疑似原生服务端）——`pip install openviking`，能跑就完全绕开 Docker+WSL

## 关键命令备忘
```powershell
# WSL（管理员）
wsl --install --no-distribution
# Docker 引擎就绪后
docker pull ghcr.io/volcengine/openviking
# 服务端文档：https://docs.openviking.ai/zh/getting-started/03-quickstart-server
# dsh 插件（必须在 Git Bash 里，TOS 镜像国内快）
bash <(curl -fsSL https://ovrelease.tos-cn-beijing.volces.com/memory-plugin-shared/install.sh) --dsh-profile web
# 验证
dsh --profile web --dump-config   # 应能看到 openviking-memory
curl http://localhost:1933/health  # 服务端健康检查
```

## 排错速查（官方文档）
| 现象 | 排查 |
|---|---|
| 没有注入/工具 | dump-config 看 openviking-memory；重跑安装器 |
| 召不回内容 | `curl http://localhost:1933/health`；prompt 是否短于 3 字符 |
| 401/403 | 检查 OPENVIKING_API_KEY / ACCOUNT / USER |
| 串入其他项目记忆 | 设 OPENVIKING_RECALL_PEER_SCOPE=actor |
| 调试日志 | 设 OV_DEBUG_LOG=/tmp/ov-dsh.log |
