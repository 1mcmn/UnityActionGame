# 答辩手册：代码地图 + 问答口径

> 用途：把"AI 生成的项目"变成"你能负责的系统"。照着这份过 3 天，你就能在不开代码的情况下画出数据流、现场改参数演示、回答"为什么这么做"。
> 原则：不写新代码，只做三件事——读结构、跑一遍流程、自己动手改一个值验证。

---

## 〇、三天计划与检验标准

| 天 | 做什么 | 用时 |
|---|---|---|
| 第 1 天 | 读第一节（全局地图）+ 第二节 A（技能编辑器窗口 12 个功能） | 1.5 小时 |
| 第 2 天 | 读第二节 B/C/D（技能运行时、播放动画、判定窗口）+ 动手改一个值 | 1.5 小时 |
| 第 3 天 | 读第二节 E/F/G（玩家、敌人、数据与音效）+ 第四节必会 20 问 + 第五节演示脚本跑两遍 | 1.5 小时 |

三天后的检验标准（能全部做到才算吃透）：

1. 不看代码，能画出"编辑器改一个连招参数 → 资产 → 运行时生效"的完整链路；
2. 能在 Unity 里现场改一个数值（伤害 / 判定帧 / 音效 ID），保存后在游戏里看到变化；
3. 能解释三个坑：资产为什么不能引用场景对象、多态内嵌序列化是什么、配置为什么会"静默失效"；
4. 能回答第四节 20 个问题里的任意 15 个。

---

## 一、全局地图

### 1.1 三层结构（论文第四章的图就是这张）

```
编辑器层（Assets/Editor，仅编辑器运行，不打包进游戏）
    └─ 技能编辑器 SkillEditorWindow（EditorWindow）
    └─ 音效工具（音效管理器 / 音频资源浏览器 / 条目管理器）
数据层（ScriptableObject 资产，编辑器与运行时之间唯一的接口）
    └─ SkillLibrary / SkillConfig（技能 = 标识 + 组件列表）
    └─ ComboData（连击参数）/ EnemyConfig（敌人参数）
    └─ SoundLibrary（soundID ↔ AudioClip）
运行时层（Assets/Scripts，只读数据层）
    └─ SkillManager + SkillInstance + 5 种技能组件
    └─ ThirdPersonController + PlayerCombat + PlayerLocomotion
    └─ EnemyAI + Enemy / GameManager / SoundManager / UI / 伤害数字
```

**层间关系一句话**：编辑器写资产，运行时读资产，两层之间没有直接调用——所以改配置不需要重新编译运行时代码。这是"数据驱动"的核心，也是答辩必答的一句。

### 1.2 一次完整链路（背下来，答辩开场就用它）

```
① 编辑器：选中技能 → 选中「伤害」组件 → 把 10 改成 25
② 写回：SerializedObject.ApplyModifiedProperties() → EditorUtility.SetDirty() → 资产落盘
③ 运行时：SkillManager.PlaySkill("player_uppercut", caster, targetPos)
④ 取数据：library.Get(id) → SkillConfig → 按顺序遍历 components
⑤ 执行：PlayAnimationComponent.OnStart 播动画 → OnTick 每帧算当前帧
        → HitWindowComponent 在判定帧区间内做体积检测 → 命中后读 DamageComponent 的伤害
⑥ 反馈：HitFeedbackComponent 播音效 + 顿帧；Enemy.TakeDamage 结算伤害与击退
```

### 1.3 文件规模与三档划分（50 个文件、8014 行）

必须讲透（约 4200 行）——就是这 6 个文件：

| 文件 | 行数 | 一句话职责 |
|---|---|---|
| `Assets/Editor/Tool/编辑器/动作工具/SkillEditorWindow.cs` | 1777 | 技能编辑器窗口：技能库、技能、组件、帧事件、预览、校验 |
| `Assets/Scripts/SkillSystem/阶段3/PlayAnimationComponent.cs` | 220 | 播放动画 + 帧事件派发 + 动画覆盖 |
| `Assets/Scripts/SkillSystem/阶段3/HitWindowComponent.cs` | 330 | 判定窗口：体积检测 + 扫掠采样 + 命中结果 |
| `Assets/Scripts/SkillSystem/阶段2/SkillManager.cs` | 203 | 技能单例：释放 / 打断 / 逐帧驱动 / 顿帧服务 |
| `Assets/Scripts/Player/ThirdPersonController.cs` | 671 | 玩家状态机 + 输入缓冲 + Root Motion |
| `Assets/Scripts/Enemy/EnemyAI.cs` | 784 | 敌人 13 态 FSM + 距离决策 + 连段 |

**能讲清即可（约 3000 行）**：`PlayerCombat`、`SkillInstance`、`SkillComponent`、其余 4 个组件、`Enemy`、`GameManager`、`SoundManager`、`CameraFollow`、`DamagePopup`、`SkillFrameUtil`、`SkillConfig` / `SkillLibrary` / `ComboData` / `EnemyConfig`、UI 脚本。

**可以淡化（约 1500 行）**：音效工具三件套（`音效管理器.cs` 724 行、`AudioBrowser.cs` 425 行、`音效条目管理器.cs` 73 行）、`PlayerStatsEditor`、`WeaponPoseTool`、IK 与练习代码。被问到就一句："配套的音效工具，负责音效库的表格化编辑、批量导入与试听。"

---

## 二、核心文件逐个拆

### A. 技能编辑器窗口 `SkillEditorWindow.cs`（1777 行，12 个功能）

**它做的事，按代码顺序：**

1. **技能库管理**：路径写死为 `Assets/SkillSystem/SkillLibrary.asset`，不存在就自动创建（`LoadOrCreateLibrary`）。
2. **技能列表**：`ReorderableList` + `SerializedObject` 绑定 `skills` 数组，显示 `[技能ID] 技能名`，点 + 新建一个 `SkillConfig` 资产。
3. **技能详情**：`skillId` / `skillName` 两个字段 + 组件列表。
4. **组件列表**：组件是 `[SerializeReference]` 多态内嵌，所以不能直接用 `PropertyField` 画——代码用 `prop.NextVisible()` 手动遍历每个组件的字段逐个绘制（`drawElementCallback` 与 `elementHeightCallback` 成对，先算高度再画）。
5. **添加组件菜单**：`GenericMenu` 提供 5 种组件——播放动画、判定窗口、伤害、命中反馈、无敌帧（`ComponentTypes` 字典）。
6. **中文标签与悬停说明**：`FieldLabelOverrides` 字典，把 `stateName`、`overrideClip`、`duration` 等字段翻译成中文并给出使用说明。
7. **绑定节点下拉**：判定盒要绑到武器骨骼，但资产不能引用场景对象，所以存"骨骼名 / 相对路径"，界面用下拉从预览目标身上枚举骨骼（`GetBonePathOptions`，手部与武器优先排序）。
8. **帧事件表格**：独立 `ReorderableList`，列是 帧号 / 音效 ID（带下拉选择）/ 音量 / 试听按钮；下方有"在第 N 帧添加事件"按钮。
9. **音效数据源**：`AssetDatabase.FindAssets("t:SoundLibrary")` 收集全项目音效 ID 用于下拉与试听；同时 `LoadRuntimeSoundIds()` 读场景里 `SoundManager` 的 `_libraries`，区分"编辑器能试听"与"游戏里真能播"。
10. **动画预览**：`AnimationMode.SampleAnimationClip` 采样 + 播放 / 暂停 / 逐帧 / 循环 + 时间轴滑动条 + 当前帧 / 总帧 / 帧率显示；播放头跨过帧事件时在编辑器里试听音效（`AuditionFrameEvents`，与运行时同一条帧跨越规则）。
11. **配置校验（重点）**：帧号越界（算出 `maxFrame = min(duration, clip.length / speed) × 帧率 × speed`）；状态名校验（读玩家 Animator 的 controller，递归 `m_AnimatorLayers → m_StateMachine → m_ChildStates / m_ChildStateMachines`，名字不存在就列出所有可用状态名）；音效 ID 运行时可用性；Humanoid 动画类型匹配。
12. **判定盒可视化**：`SceneView.duringSceneGui` 里画球 / 胶囊 / 盒线框，并提供半径、偏移、旋转三个拖拽手柄，拖动直接写回资产。

**一定会被追问的 6 个点：**

- **为什么不用 `UnityEditor.Animations.AnimatorController`？** → 编辑器程序集没有引用 `UnityEditor.AnimationModule`，直接用会 CS0246；改成用 `SerializedObject` 读 controller 的序列化字段，零额外依赖。
- **`[SerializeReference]` 是什么、为什么用它？** → Unity 的多态序列化：一个 `List<SkillComponent>` 里可以放不同类型的子类实例。不用它就只能给每个组件建独立资产文件，策划要管一堆文件。
- **为什么资产里存骨骼"名字"而不是 Transform？** → ScriptableObject 资产不能引用场景对象（引用会被清空），这是资产与场景的生命周期隔离；运行时用 `ResolveNodeStatic` 按名字或路径解析。
- **"静默失效"是什么？** → 数据驱动架构里，配置对不上不会抛异常，只表现为功能不生效：帧号配到不会走到的帧、状态名拼错、音效 ID 不在运行时库里，Console 一声不吭。这三类现在都被编辑器红字拦住。
- **预览为什么不是最终效果？** → 预览只是配帧工具（`AnimationMode` 采样），不写进资产；游戏里播哪个动画由组件里的 `stateName` 决定。
- **编辑器代码怎么保证不被打包？** → 编辑器脚本放在 `Assets/Editor` 下，只编进 `Assembly-CSharp-Editor`；运行时脚本用 `Game.asmdef` 隔离。

### B. 技能运行时 `SkillManager.cs` + `SkillInstance.cs` + `SkillComponent.cs`

- **生命周期四态**：`None → Released → Running → Ended / Interrupted`。`SkillInstance.RequestFinish()` / `RequestInterrupt()` 只改状态，真正的回调由 `SkillManager.Update` 统一处理（`ProcessEnd` / `ProcessInterrupt`）。
- **组件接口四回调**：`OnStart` / `OnTick` / `OnEnd` / `OnInterrupt`——"打断也要清理"是这套设计的关键：判定窗口在 `OnInterrupt` 里关闭激活状态，无敌帧在 `OnInterrupt` 里复位，避免"技能被打断了还在继续判定 / 永久无敌"。
- **同 ID 唯一**：重复释放同一个技能会先 `InterruptSkill(id)` 再新建，防止实例叠加；另有 `interruptOthers` 参数与 `SwitchSkill(from, to)` 用于"架势 → 振刀"这类互斥切换。
- **顿帧服务**：`RunHitStop(duration, timeScale)` 用协程改 `Time.timeScale` 再恢复（组件是纯数据对象，没有 MonoBehaviour，持续型行为必须委托给 Manager）。

**追问与答法：**

- *为什么不把技能做成继承？* → 组合优于继承：新增技能 = 在编辑器里加几个组件，不改代码。
- *打断时怎么保证没有残留？* → `OnInterrupt` 与 `OnEnd` 成对清理，判定区关闭、无敌帧复位。
- *技能的"当前帧"从哪来？* → `PlayAnimationComponent` 每帧把当前帧写进 `ctx.runtimeData["CurrentFrame"]`，判定窗口与无敌帧组件都读它——所以组件顺序有约束：**播放动画必须排在判定窗口之前**。

### C. `PlayAnimationComponent.cs`（帧事件 + 动画覆盖）

- **数据**：`stateName`（动画机状态名）、`crossFade`（过渡秒数）、`duration`（技能时长，小于等于 0 表示不自动结束）、`frameEvents`（帧号 + 音效 ID + 音量）、`overrideClip` / `baseClip`（动画覆盖）。
- **运行**：`OnStart` 里 `CrossFadeInFixedTime(stateName)`；`OnTick` 里读 `normalizedTime` 算当前帧，帧跨越判定写死为 `evt.frame > lastFrame && evt.frame <= currentFrame`——这一行就是"不漏触发、也不重复触发"的实现（跳帧时中间的事件会补触发）。
- **帧率统一**：帧号换算全部走 `SkillFrameUtil`，帧率取 `clip.frameRate`（取不到才用 30），不再硬编码 30fps。
- **动画覆盖**：`ApplyAnimationOverride` 用 `AnimatorOverrideController` 把 `stateName` 状态原本的动画换成 `overrideClip`。三个坑：① 玩家 Animator 本来就挂着 OverrideController，不能重复套壳 → 用静态字典记住"最初的 controller"；② 套壳会丢掉原有覆盖 → `GetOverrides` / `ApplyOverrides` 继承；③ 每次 new 会累积垃圾 → 按 (controller, baseClip, overrideClip) 缓存。

### D. `HitWindowComponent.cs`（判定窗口）

- **数据**：`startFrame` / `endFrame`（判定帧区间）、`shape`（Sphere / Capsule / Box）、`radius` / `capsuleLength` / `boxHalfExtents` / `offset` / `rotationOffset`、`attachNodeName`（绑定骨骼）、`targetMask`（层掩码，默认 128 = 敌人层）、`damageComponent` / `feedbackComponent`（引用内嵌组件）。
- **两种判定路径**：没绑骨骼 → 以角色根节点为中心的球（旧行为）；绑了骨骼 → 判定盒跟随骨骼，并在"上一帧变换 → 这一帧变换"之间插值采样 3 次（`SweepSamples = 3`），把挥砍扫过的体积都查一遍，解决高速挥砍漏判。
- **去重**：进入窗口时清空 `hitTargets`，同一次挥击对同一目标只结算一次。
- **命中结果**：`HitResolver.Resolve()` 通过 `IHitReactable` 接口问目标"这一下算什么"，返回 `Hit / Blocked / Parried / Invincible`；没人实现接口时默认正常命中——这是零侵入设计：现有敌人与玩家代码不改也能跑，等接了接口格挡弹反立刻生效。

**追问与答法：**

- *为什么不用 SphereCast？* → 任务书写的是"SphereCast 或 Capsule Collider"，我们用的是 `Physics.OverlapSphere` / `Physics.OverlapCapsule` 体积检测加帧间扫掠采样：动画驱动的判定里，武器节点两帧之间位移可能很大，单次 Cast 容易漏掉端点之间的区域，插值采样等价于把"这一帧扫过的体积"都测一遍，是升级不是偏离。
- *为什么判定窗口用帧不用时间？* → 动画是帧驱动的，用帧号能和动画严格对齐；不同帧率素材通过 `SkillFrameUtil` 换算。

### E. 玩家侧 `ThirdPersonController.cs` + `PlayerCombat.cs`（验证平台）

- **10 个状态**：Idle / Move / Run / Dodge / Attack / Parry / Block / Hit / Knockdown / Dead，`ChangeState()` 统一处理进入与退出。
- **输入缓冲**：`_inputBufferDuration = 0.1f`（**注意是 0.1 秒**，别按网上流传的 0.18 秒说），缓冲期内按下攻击，会在当前动作结束后立刻接上。
- **位移是"按状态分两套"的**（被问到一定要说准）：移动状态（Move / Run）由 `PlayerLocomotion` 通过 `rb.velocity` 驱动，`Update` 只处理相机相对转向；攻击 / 闪避 / 受击等动作状态由 `PlayerAnimController.OnAnimatorMove()` 把动画的 `deltaPosition` / `deltaRotation` 应用到刚体来驱动位移（`rb.MovePosition` / `rb.MoveRotation`）。进入动作状态时 `PlayerLocomotion.Halt()` 会把水平速度清零，避免两套位移叠加打架。答法："移动用速度驱动、动作由根运动驱动，切换时清速度，保证看到的位置和实际位置一致。"
- **连招（`PlayerCombat`）**：`ComboWindowPercent`（连击窗口，用动画进度比例表示）+ `ComboGraceWindow`（残响窗口，攻击计时结束后仍可续招的兜底）+ `comboDamages[]`（各段伤害）。动画事件链是 `PlaySfx("atk01_swing")` → `PerformHitDetection` → `PlayHitSfx` → `TriggerHitStop`，由 `AnimationEventRelay` 从动画转发出来。
- **数据来源**：以上参数都在 `ComboData` 资产里（`attackDuration` / `attackRadius` / `comboWindowPercent` / `comboGraceWindow` / `maxComboStep` / `comboDamages`）。

### F. 敌人侧 `EnemyAI.cs`（13 态 FSM）

- 状态：Spawn → Idle → Walk → RunStart → Run → RunEnd → Attack（7 段）→ Staggered → StunStart → StunLoop → StunEnd → StunHit → Death。
- 决策：按与玩家的距离切换待机、接近、追击与攻击，攻击有连段递进与冷却。
- **要说实话的地方**：动画资源目前只覆盖了 `Attack_01`、死亡与眩晕等状态，`Attack_02~07` 在动画机里还没有对应动画，`CrossFade` 会失败并打一条 warning。答法："敌人 FSM 的逻辑是完整的，动画素材目前只覆盖第一段与受击、死亡，其余段落的动画在后续补齐。"主动说比被问出来好。

### G. 数据资产与配套系统（快速过）

- `SkillConfig`：`skillId` / `skillName` / `List<SkillComponent> components`（`[SerializeReference]`）。
- `SkillLibrary`：`List<SkillConfig> skills` + `Get(id)` 线性查找。
- `SkillFrameUtil`：帧与时间换算的唯一出处（`TimeToFrame` / `FrameToTime` / `TotalFrames` / `SnapTimeToFrame`）。
- `SoundManager`：单例 + 字典查表 + `AudioSource` 对象池 + `PlayByPrefix`（前缀随机，如 `foot_step` 随机取 `foot_step_01~08`）+ 动态 pitch。
- `GameManager`：单例 + 局内状态（开始界面 / 进行中 / 结算），订阅玩家与敌人死亡事件，`EndGame(victory)` 与 `Restart()`。
- `GameLog`：日志门控，控制运行时输出量。
- **测试现状**：EditMode 共 4 组 15 个用例——`ComboDataTests`(3)、`EnemyConfigTests`(2)、`PlayerCombatTests`(7)、`SoundLibraryTests`(3)；目前没有技能系统的单元测试。

---

## 三、三个"踩坑故事"（回答"你遇到什么困难"）

用 STAR（现象 → 根因 → 方案 → 结果）讲：

**1. 配置"静默失效"（最推荐讲）**

现象：编辑器里配了第 27 / 50 帧的音效，游戏里完全不响，Console 没有任何报错；但编辑器里点试听能听到。根因：技能时长只有 0.5 秒，按 30fps 最多走到第 15 帧，那两个事件永远走不到；同类的还有状态名拼错、音效 ID 不在运行时音效库里。方案：把校验前移到编辑器，做成红字提示（帧号越界、状态名不存在、音效 ID 运行时不可用）。结果：三类静默失效变成编辑器里可见的错误。收尾一句："数据驱动架构的代价是错误会变安静，所以编辑期校验和运行时逻辑同等重要。"

**2. 资产不能引用场景对象**

现象：想把武器骨骼的 Transform 拖进技能资产，拖不进去（或拖进去被清空）。根因：ScriptableObject 是项目资产，场景对象是场景实例，二者生命周期不同，Unity 不允许资产引用场景对象。方案：资产里存骨骼名或相对路径，运行时用 `ResolveNodeStatic` 解析成 Transform。结果：判定盒能跟随武器骨骼，同时资产保持可复用。

**3. 多态内嵌序列化的删除坑**

现象：在编辑器里删组件，列表里留下"空槽位"，数量不减。根因：`DeleteArrayElementAtIndex` 对引用型数组元素是"先置空、再删一次才缩短数组"，只调一次就留下 null 元素（资产里表现为 `rid: -2`）。方案：删除时空元素连续删两次，或在界面提供"清理空槽"。结果：配置列表保持干净；这条也可以作为"已知问题与改进方向"写进论文。

---

## 四、必会 20 问（30 秒答法）

1. **你的课题做什么？** → 做一个 Unity 编辑器工具（连招 / 技能配置编辑器），加一个动作游戏 Demo 作为验证平台；工具负责把战斗参数从代码里搬到可视化配置里，并做编辑期校验。
2. **为什么选这个题？** → 动作游戏的连招参数时序性强、约束多、错了不报错；硬编码导致改一次要编译一次，非程序人员不能参与。
3. **三层架构是什么？** → 编辑器层（写资产）、数据层（ScriptableObject，唯一接口）、运行时层（读资产）。
4. **"编辑器改了就生效"怎么实现？** → 编辑器用 SerializedObject 写资产并 SetDirty，运行时只读资产；两层没有直接调用，所以不用重新编译。
5. **技能的数据结构？** → 技能 = 标识 + 组件列表，组件用 `[SerializeReference]` 多态内嵌，五种组件：播放动画、判定窗口、伤害、命中反馈、无敌帧。
6. **为什么用组件而不是继承？** → 组合优于继承；新增技能是配数据，不是写代码。
7. **判定怎么做的？** → 判定帧区间内做球 / 胶囊体积检测，绑定武器骨骼，并做帧间扫掠采样防漏判；命中结果通过 `IHitReactable` 接口问目标（普通命中 / 格挡 / 弹反 / 无敌）。
8. **帧事件怎么保证不漏不重？** → 帧跨越判定 `evt.frame > lastFrame && evt.frame <= currentFrame`，跳帧也能补触发，同一帧只触发一次。
9. **帧率怎么处理？** → 统一走 `SkillFrameUtil`，取 `clip.frameRate`，所以 24 / 30 / 60fps 素材都能对齐。
10. **编辑器做了哪些校验？** → 帧号越界、状态名不存在、音效 ID 运行时不可用、Humanoid 动画类型不匹配。
11. **什么是"静默失效"？** → 配置错误不抛异常，只表现为功能不生效（见第三节故事 1）。
12. **为什么编辑器不用 UI Toolkit？** → 任务书指定技术栈是 EditorWindow / SerializedObject / ReorderableList（IMGUI 体系），且需要大量自绘（时间轴、判定盒线框），IMGUI 的 Rect 与 Handles 更直接。
13. **编辑器代码会不会打包进游戏？** → 不会，放在 `Assets/Editor`，只编进编辑器程序集。
14. **怎么调试配置问题？** → 编辑器红字 + 运行时日志门控（GameLog）+ Console 里的技能释放与打断日志。
15. **测试怎么做的？** → EditMode 单元测试 4 组 15 个用例（数据层、战斗逻辑、音效库）+ 命令行编译门禁；技能系统的配置校验在编辑器侧覆盖，单元测试在计划里。
16. **动画覆盖是什么？** → 用 `AnimatorOverrideController` 把某状态原本的动画换成指定的 clip，策划不用去动 Animator。
17. **游戏部分算什么？** → 验证平台：提供真实的配置对象和验证场景，用来证明"编辑器配置 → 游戏生效"，不是研究对象。
18. **你最大的收获 / 最难的地方？** → 定位"不报错的错"（见第三节故事 1）。
19. **工作量与创新点？** → 数据驱动的战斗配置 + 帧级时序统一 + 编辑期配置校验；把主观手感拆成可度量参数。
20. **哪些还没做完？** → 任务书要求的角色跳跃、敌人巡逻、连击数界面在计划中；技能系统的单元测试在计划中；敌人部分动画素材待补齐。（主动列出来，比被问出来强）

---

## 五、现场演示脚本（5 分钟，提前跑两遍）

1. **开场一句话**："我的课题是开发一个 Unity 编辑器工具，游戏是验证它能不能用的平台。"
2. 打开 `Tools → 技能编辑器`，选中技能，展示技能 ID / 名称 + 组件列表（说明"技能 = 标识 + 组件装配"）。
3. 选中「伤害」组件，把伤害值从 10 改成 25，点"保存所有修改"，并说明这一步发生了什么（SerializedObject 写回资产）。
4. 选中「播放动画」组件，展示 stateName 校验的绿字（"stateName 校验通过 → clip「AT1」"），然后故意改成 `AT1x`，出现红字"该状态名不存在"，再改回来。这一步专门展示"配置校验把错误拦在编辑期"。
5. 帧事件表格：把事件帧号改成 999，出现红字提示"永远不会触发"并告诉你这个技能最多能走到第几帧，再改回来。
6. 场景视图里选中「判定窗口」组件，拖判定盒的半径与旋转手柄，说明判定盒跟随武器骨骼。
7. 关掉编辑器窗口，进游戏按右键放技能，展示伤害数字的变化（对应第 3 步改成 25 的那个值）——"编辑器改配置，运行时立即生效"。
8. 收尾："工具是研究对象，游戏是验证手段，两边通过 ScriptableObject 资产解耦。"

---

## 六、口径校正与风险提示（自己先记住）

1. **输入缓冲是 0.1 秒**，不是 0.18 秒——以代码为准（`_inputBufferDuration = 0.1f`）。
2. **敌人动画只覆盖了一部分状态**（`Attack_01`、死亡、眩晕），`Attack_02~07` 会 CrossFade 失败；别在答辩里说"敌人 7 段连击全部实现"。
3. **技能系统目前没有单元测试**，但开题报告里写了"覆盖技能查找、生命周期顺序、打断清理、帧跨越边界"。两个选择：补 4 个测试（约 2 小时，推荐），或者答辩时说"技能系统的测试在计划里，当前覆盖的是数据层与战斗逻辑"——不能让报告与现场说法打架。
4. **编辑器已知缺陷**（老师现场点到时）：删除组件会留空槽、新建技能 ID 都是 `new_skill` 不做查重、帧事件表格的音量字段运行时不生效。统一答法："这是开发过程中发现的已知问题，论文的展望里写了改进方向（删除时清理空引用、ID 唯一性校验、音效音量接入 SoundManager 重载）。"
5. **场景里 SoundManager 挂的是哪个音效库**，需要你自己在 Unity 里确认一眼（项目里有两个库：`PlayerSounds` 有 24 条音效，`GameSoundData` 只有 1 条）。挂错就会出现"编辑器能试听、游戏没声音"。
6. **AI 辅助这件事**：如果老师问，如实说"开发过程用了 AI 辅助，架构选型、需求裁剪、调试定位与验证是我做的"，然后立刻用演示证明——现场改参数、解释一段代码、说清一个取舍。不要否认，也不要主动写成"全部由 AI 生成"；真正的风险是"答不上为什么"。

---

## 七、两个版本的开题报告：范围划分与归位话术

| 版本 | 文件 | 正文汉字 | 页数 | 用途 |
|---|---|---|---|---|
| 完整版 | `开题报告_邬锦飞.docx` / `259400232_邬锦飞_开题报告.pdf` | 约 6000 | 10 | 内容最全，含技能系统、音效、帧事件等扩展描述 |
| 收敛版 | `开题报告_邬锦飞_4000字版.docx` / `259400232_邬锦飞_开题报告_4000字版.pdf` | 约 4350 | 8 | 只写任务书要求的功能，**推荐作为答辩口径** |

**收敛版保留（任务书硬要求）**：连招配置编辑器（EditorWindow、连招步骤增删排序、段间转换规则）、每段 6 类参数（动画片段、动画事件、判定起止时间、伤害值、连招窗口时间、位移参数）、配置校验与提示、ScriptableObject 持久化与菜单/快捷键重新加载、三条核心业务规则、技术指标 6 条、游戏验证平台 6 项功能（走跑跳与混合树、三段以上连招、动画事件驱动判定、受击反馈、球/胶囊判定、巡逻追击、血量与连击数 UI）、进度表 12 项与起止日期、任务书指定的 5 篇文献。

**收敛版删除（任务书之外，答辩不必主动讲）**：技能系统的组件化装配与 `[SerializeReference]` 多态内嵌、帧事件音效与无敌帧、动画覆盖（AnimatorOverrideController）、判定盒 Scene 视图可视化与拖拽手柄、音效管理器与音频裁剪工具、弹反/格挡/击倒起身、伤害数字、Boss 血条、胜负结算，以及判定扫掠采样的技术细节与产品案例堆砌。

**范围外的三句归位话术**（被追问时用，每句不超过 20 秒）：

1. 问"技能编辑器是什么" → "连招配置编辑器是我按任务书做的正式工具；早期我用一套技能数据验证过数据驱动链路，连招配置是在这条链路上收敛出来的，论文里按任务书的连招口径写。"
2. 问"音效工具 / 伤害数字 / 弹反呢" → "那是开发过程中为验证战斗反馈顺带做的配套内容，不属于本课题的研究范围。"
3. 问"为什么没做巡逻 / 跳跃" → "这两项在任务书的计划里，目前实现到追击与走跑切换，剩余部分在论文的展望里说明。"
